# 引き継ぎ 2026-08-18 ── 🔖 1台目の「済」スタンプが切れていた（PitFlow **v1.126.0**）

> 🔴 **ゆうたはコードを読まない。** 話すときはファイル名・関数名・行番号を出さない。

---

## 🗣 依頼

> 「細かいところだけど　**車検予定の済みマークが決定のラベルの下に来ちゃってる**　1台目に設置したカード
> 　これを**重ね順上にできる**？」

---

## 🔎 何が起きていたか

「済」の赤いスタンプは、**チップの角から 7px 外へ出る**作り（v0.124.2）。

- 2枚目・3枚目のカードは、**上にカードがある**のでスタンプはその上に乗って見える。
- ところが **1枚目だけは上に何も無い**。マスに `overflow:hidden` がかかっていたので
  **上の 7px が切られ**、「決定」の帯の下に潜って見えていた。

＝ **重ね順の問題ではなく「切られていた」**（測ったら、スタンプの上端がマスの上端より 3px 上）。

---

## 🔴 直したこと

- **決定の行だけ、はみ出しを切らない**（`.shk-decrow .shk-sc{ overflow:visible }`）。
- あわせて **決定の行を「決定」の帯より上に重ねる**（`position:relative; z-index:1`）。
  ⚠ 切らないだけだと、今度は帯の背景に隠れる。**この2つはセット。**
- ⚠ 横に漏れる心配は無い＝名前・車名・拡張は**それぞれの中で「…」に切っている**。

---

## 🗂 触ったところ

| どこ | 何 |
|---|---|
| `pitflow\css\shaken.css` | `.shk-decrow` に `position:relative; z-index:1`／`.shk-decrow .shk-sc{ overflow:visible }` |
| `pitflow\index.html` | 版3か所を **v1.126.0**、`shaken.css?v=21` |
| `pitflow\test_shaken_ops.mjs` | 見張り **+5本**（47 OK） |

⚠ **JS は触っていない。**（CSS だけ）

---

## ✅ 確かめたこと

`test_shaken_ops.mjs` ＝ **47 OK / 0 NG**（+5本）
- 済のスタンプが出る／1枚目はマスの上にはみ出す（作りどおり）
- 🔴 **決定の行ははみ出しを切らない**／🔴 **決定の行が「決定」の帯より上に重なる**
- 2枚目のスタンプも今までどおり

画面でも、1枚目・2枚目とも赤い「済」が欠けずに出ていることを見た。
ほかも緑＝`test_shaken_unsched` 48 ／ `test_shaken_decide` 33 ／ `test_shaken_detail` 25 ／
`test_order_wash` 28 ／ `test_shaken_share` 27。

---

## 🚀 出しかた

```powershell
powershell -File "D:\Claude\アプリ開発\deploy-pitflow.ps1" -Message "v1.126.0 車検予定＝1台目の「済」スタンプが切れていたのを直す"
```

⚠ `_shared` と `firestore.rules` は触っていない。MHS・CarFlow も触っていない。

### 📣 お知らせ
**足していない**（見た目の直しなので）。

---

## ⏭ 次にやること

1. 済にする前の予約詳細にも陸運局とRを出すか／MHS・前日LINEにも出すか。
2. 🔴 未入庫（来なかった車）になると車検予定から黙って消える（v1.115.0 から持ち越し・要確認）
3. 前からの持ち越し（`mydash.js` の車検BOX／予約詳細の「陸運局休」／LINE画像の自動試験なし）
