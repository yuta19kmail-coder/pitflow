# 引き継ぎ 2026-08-20 ／ PitFlow **v1.158.0**
## 🔴🔴 **預かり0日（＝当日返し）でも、代車は「1日利用」として押さえる**

> 🗣 ゆうた「**0日あり得る。いって帰ってくるだけで代車使いたいと。
> 　　代車的には1日利用として存在する**」

---

## 🔎 なにが起きていたか

**`0` を「まだ決まっていない」と同じ扱いにして、`1週間` の窓を出していた。**

- お客様「**朝入れて、夕方には取りに来る。でも代車は貸してほしい**」
  ＝ 泊まらない（0泊）けれど、**代車はその日1日ぶん、ちゃんと要る**
- なのに案内は **「1週間まるごと空いている日」からしか出さなかった**
  ＝ 🔴 **本当なら今日から貸せるのに、1週間先を案内していた**
- しかも **エラーは1つも出ない**。数字が静かに間違っているだけ

原因は3か所で **`0` を落としていた**こと。

| どこ | 前 | なにが悪いか |
|---|---|---|
| `loaner-free.js` `planNeed` | `if (!isFinite(h) \|\| h <= 0) → 1週間` | **0 も 未選択もまとめて弾いていた** |
| `sales-count.js` `planHoldOf` | `return v > 0 ? v : null` | **0 を null（未決定）に潰していた** |
| `dashboard.js` `dashEarliestIntake` | `(holdOverride && +holdOverride > 0) ? … : null` | 同上（`0` は falsy なので消える） |

---

## ✅ どう直したか ─ **`0` と「未選択」は別物、と決めた**

🔴🔴 **これがこの版のいちばん大事な決めごと。**

| `hold` | 意味 | いる窓 | 画面の言葉 |
|---|---|---|---|
| `null` / `''` | **まだ決まっていない**（作業タイプ未選択） | **7日**（1週間きっちり） | 「1週間が取れる日」 |
| 🔴 **`0`** | **当日返し＝代車は1日** | 🔴 **3日**（前日・当日・翌日） | 🔴 **「当日返し（代車1日）＋前後1日が取れる日」** |
| `1以上` | その日数 | 日数＋前後1日 | 「預かり5日＋前後1日が取れる日」 |

- **代車を使う日数 ＝ `max(預かり日数, 1)`**（`loaner-free.js` の `PLAN_MIN`）
  ＝ **0泊でも、代車は1日ぶん存在する**
- ＝ **0日 と 1日 は同じ幅**（どちらも代車を1日使う）
- 🔴 **数字だけ変えない。** 画面の札にも **「当日返し（代車1日）」** と出して、
  なぜその日なのかが分かるようにした（v1.156.0 の決めごとと同じ）

### 「待ち」「当日仕上げ」も 0 になる

`pitEstHold` は 受付タイプが **待ち・当日仕上げ** のとき 0 を返す。
前はそれも null に潰していたので **1週間の窓**だった。
いまは **0＝代車1日** になる。
＝ **代車必要にチェックが入っているなら、1日ぶん押さえて案内する**（これが現場の実態）。

---

## 🧭 触った所と、触っていない所

| | |
|---|---|
| 変えた | **案内用の窓の決め方**（`planNeed`）と、そこへ渡す値の拾い方2か所 |
| 変えていない | 🔴 **`pitSalesHoldOf`（売上用・最後に必ず 5 を返す）** ＝ 別物。混同しない |
| 変えていない | 実際に貸せるか・ぶつかりの判定（`busyOn` / `conflicts`）。ここは**案内専用** |
| 変えていない | `pitHoldDays`（泊数の数え方・当日返しは0）＝ **その決めごとはそのまま** |

🔴 **判定は `loaner-free.js` の `pitLoanerPlanNeed` / `pitLoanerPlanWindow` 1本。**
画面ごとに「0のときどうする」を書き写さないこと。

---

## 🧪 見張り

`test_short_intake.mjs` を **53 → 65 OK / 0 NG**。足した／直したのは：

- 🔴🔴 **0日の窓は3日**（前日・当日・翌日）。位置も確かめる
- 🔴 **0日は「1週間」に化けない**（未選択と混ぜない）
- 🔴 **当日返しなら今日から案内できる**／**未選択のままなら今日は案内しない**
- 🔴 **最短入庫日も 0日 と 未選択 で変わる**／**0日と1日は同じ幅**
- 🔴 `pitCardHoldDays` が **手で 0 と入れたら 0 を返す**（null に化けない）
- 🔴 **待ち・当日仕上げは 0**（前は null＝1週間だった）
- 🔴 実物の画面で **札も帯も「当日返し」と言っている**／**帯が3日ぶん**

### 🔍 見張り98本を全部通した

**このセッションで壊した所は無し。** 落ちているのは**前から落ちていた7本だけ**で、
v1.148.0（このセッション前）と**同じ数字**：
`test_custform` 6NG ／`test_order_wash` 6NG ／`test_board_order` 1NG ／
`test_loaner_name` 2NG ／`test_loaner_note` 1NG ／`test_kb_toolbar` 1NG ／
`test_print_staff` 1NG ／`test_nav` は動かない。**ほかの90本は 0 NG。**

---

## 次にやること（持ち越し）

- ⏭ **前から落ちたままの7本**を一度まとめて片付けたい。
  🔴 **落ちたままだと「増えたか減ったか」が分からなくなる。**
- ダッシュボードの「代車の最短空き」は**1日空きのまま**（`dashLoanerEarliestFree`・別の指標）。
- 商用車（`commercial`）の代車は**国産あつかい**。分けるなら決めごとが要る。
- `CoreFlow_エラー番号一覧.html` の作り直し（`PF-4011`／`PF-3034`）。

---

## デプロイ

```
powershell -File "D:\Claude\アプリ開発\deploy-pitflow.ps1" -Message "v1.158.0 預かり0日（いって帰ってくるだけ）でも代車は1日利用として押さえる／0と『まだ決まっていない』を別物にした"
```
