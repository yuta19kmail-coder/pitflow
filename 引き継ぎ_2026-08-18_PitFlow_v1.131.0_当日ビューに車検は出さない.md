# 引き継ぎ 2026-08-18 ／ PitFlow **v1.131.0**（＋ MHS **v1.32.0**）
## 当日ビューに車検は**出さない**／MHS の当日の車検を**センターで切って2カラム**

> 🗣 ゆうた「**ごめん　ピットの当日は車検はないか**」
> 🗣 「でも MHS の方は**車検多い時は8台**とかになるのと、**画面のワイド次第でナンバー、担当等がかなり離れて**
> 　　表示されるので、**画面をセンターで切って2カラム分表示で切る**ようにして」

⚠ **同じ日に出した v1.130.0 の続き（訂正）。**
　 v1.130.0 は「当日ビュー**と** MHS の両方に車検の1行を出す」だったが、
　 **当日ビュー側は取り消し**、MHS 側だけ残して**読みやすく作り直した**のがこの版。

---

## ① 当日ビューの車検の枠は**取り消した**（PitFlow v1.131.0）

- `js/today.js` の `_todShakenHtml()` と、`css/views.css` の `.today-shk` / `.tshk-*` を**丸ごと消した**。
- 🔴 **当日ビューは入庫・返車を見るところ。**車検は**車検予定の画面**と、工場の **MHS** で見る。
- 🔴 **ここに足し直さないこと。** `test_today_shaken.mjs` が「出ていないこと」を見張っている
  （黙って足すと試験が落ちる）。

### ⚠ ただし**物差しは消していない**
`pit-share.js` の **`PIT_SHAKEN_KIND`** と **`pitPlate4`**、`pitShakenOnDate` が配る
**`kind` / `plate4`** は **v1.130.0 のまま残してある**。

> 🔴 **理由＝MHS の当日がこれを借りているから。**
> 　 「当日ビューで使わなくなったから消そう」とすると、**工場の画面が黙って古い形に落ちる**
> 　 （MHS は借りるものが1つでも欠けると丸ごと自前の写しに戻る作り。エラーは出ない）。

---

## ② MHS の当日の車検を**2カラム**に（MHS v1.32.0）

### 🔎 何が起きていたか
1. 横1本の行なので、**ワイド画面ほど** 客名 …………………… ナンバー と**離れて目で追えない**。
   （車種に `flex:1` が効いていて、右端まで押し広げていた）
2. **8台になると縦にも伸びて**、他の枠が下に押し出される。

### ✅ 直し
- 車検の行を `.bshk-list` で包み、**`column-count:2`** で**真ん中に区切り線を入れて左右に割る**。
- 🔴 埋まる順は **左の列を上から下 → 次に右の列**。
  並びは物差し（午前が先・午後が後）のままなので、**左に午前・右に午後**が来て読みやすい。
  ⚠ **左右に振り分ける並べ替えはしていない。**並びは PitFlow の物差しのまま。
- 🔴 **3台までは1カラムのまま**（2台を左右に離すと、かえって目が泳ぐ）。4台以上で2カラム。
- 🔴 半分の幅になっても離れないよう、**2カラムの時だけ車種を伸ばすのをやめた**（`flex:0 1 auto`）。
  → 種類・AM/PM・客名・車種・ナンバー・担当・陸運局・R が**左に固まって並ぶ**。
- 🔴 **縦フルビュー（サイネージ）は1カラムのまま。**横が狭いので割ると読めない。

### 📐 実測（1680px・8台）
```
枠幅の半分ずつに分かれる／左4行・右4行／となり同士のすき間は最大でも 7px
1280px・5台・長い車種（ランドクルーザープラド TX-Lパッケージ）でも
  折り返しなし・はみ出しなし・「未定 回送・陸運局・R」も収まる
```

---

## 🏷 頭の「車検」は**1行ごとの種類**（v1.130.0 から変えていない）

ゆくゆく**名変・構造変更**などが同じ枠に混ざる前提。
種類は `pit-share.js` の **`PIT_SHAKEN_KIND`** 1か所で決まり、`row.kind` として配られる。
🔴 **MHS の中に '車検' と直接書かない。**書くと PitFlow 側を直しても MHS だけ変わらない。

---

## ✅ 見張り（試験）

| ファイル | 中身 | 結果 |
|---|---|---|
| **`test_today_shaken.mjs`（作り直し）** | 🔴当日ビューに車検を**出さない**／`pitPlate4`（0078を78にしない・文字列のまま・無ければ空）／物差しが `kind`・`plate4`・担当・陸運局・R を配る | **16 OK** |
| `test_shaken_unsched.mjs` | 未入庫の3つの箱 | 48 OK |
| `test_shaken_ops.mjs` | クリックとドラッグ | 54 OK |
| `test_shaken_decide.mjs` | 決定の窓（担当・陸運局・R） | 36 OK |
| `test_shaken_detail.mjs` | 予約詳細の車検 | 41 OK |
| `test_order_wash.mjs` | 受注のときの洗車 | 28 OK |
| `test_shaken_share.mjs` | 物差し1本 | 27 OK |

MHS 側は `MHS\probe_shk2col.mjs`（**本物の index.html を読み込んで実際に測る**）で 10 OK。
`probe_shk_edge.mjs` は長い車種・狭い画面での折り返し確認用。
⚠ **MHS は「関数を呼んで通った」では足りない。**画面に**実際に描かせて位置を測る**こと
（2026-08-18 のドラッグの件と同じ轍）。

---

## ⏭ 次に考えること（頼まれていない。相談してから）

- **名変・構造変更などの種類を実際に足す**とき＝`pit-share.js` の `kind` の決め方1か所。
  そのとき、**どの作業種別（`workTypes`）を何と呼ぶか**をゆうたに決めてもらう。
- 前日LINEの画像にも下4桁・陸運局・R を出すか（いまは出していない。**画像は自動試験が無い**）。
- `mydash.js` の車検BOXは触っていない。
- 済にする前の予約詳細にも陸運局とRを出すか（v1.120.0 からの持ち越し）。
- 未入庫（来なかった車）が車検予定から黙って消える件（v1.115.0 からの持ち越し）。

---

## 📌 触ったファイル

```
PitFlow\pitflow\js\today.js            ← 車検の枠を取り消し
PitFlow\pitflow\css\views.css          ← .today-shk / .tshk-* を取り消し
PitFlow\pitflow\index.html             ← v1.131.0（3か所）＋ today.js?v=43 / views.css?v=50
PitFlow\pitflow\test_today_shaken.mjs  ← 作り直し（出さないことの見張り＋物差し）
PitFlow\pitflow\js\pit-share.js        ← ⚠ v1.130.0 のまま。消さないこと（MHS が借りている）
MHS\index.html                         ← 2カラム化／v1.32.0（3か所）
MHS\probe_shk2col.mjs・probe_shk_edge.mjs（新・目で見る用）
```
