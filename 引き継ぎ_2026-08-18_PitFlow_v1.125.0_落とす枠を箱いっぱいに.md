# 引き継ぎ 2026-08-18 ── 🎯 落とす枠を「その日の午前／午後の箱いっぱい」に（PitFlow **v1.125.0**）

> 🔴 **ゆうたはコードを読まない。** 話すときはファイル名・関数名・行番号を出さない。
> ⚠ **v1.123.0 → v1.124.0 → これ**で1つの話。**本当の原因はこれだった。**

---

## 🗣 依頼（ゆうたが自分で原因を突き止めた）

> 「わかった　**決定の行く車のカードにに乗せないとだめだった**
> 　**ドラッグ先の枠をその日のAMPMの枠いっぱいにとってほしい**
> 　でさっきまでの修正を含めて**ちゃんと綺麗にやり直して**ほしい」

---

## 🔎 本当の原因 ── **落とす枠が「中身の高さ」しか無かった**

決定の行は、いちばん台数が多い日の高さになる。ところが**枠（当たり判定）は中身の分だけ**だった。

- 他の日に5台入って行が **300px** になっていても、
  **空いている日の枠は上のほうの 42px だけ**。
- だから「**カードの上に正確に乗せないと落ちない**」。ゆうたの見立てが完全に正しかった。

⚠ v1.123.0（チップを細く）と v1.124.0（決定の行を画面に出す）は**どちらも必要な直し**だったが、
　 **これが本丸**だった。3つ合わせて、やっと素直に動く。

---

## 🔴🔴 直したこと（3つまとめて整理した）

### ① 落とす枠は **その日の午前／午後の箱いっぱい**（本丸）
- 決定の行だけ、マスを縦のflexにして枠を伸ばした（`.shk-decrow`）。
  ⚠ `.shk-sc` は AM/PM の見出し行でも使うので、**この行の中だけ**に効かせること。
- 🔴 **カードの上でなくてよい。箱のどこでも落ちる。**

### ② ゴースト（落ちる先の薄いチップ）を**浮かせた**
- 前は枠の中に入れていたので、掴んでいる最中に**表が伸び縮みして狙った枠が動いていた**。
- `position:absolute` にして、**高さを変えない**ようにした。

### ③ ブラウザ標準のドラッグの受け口を**全部消した**（掃除）
- v0.124.1 で**使うのをやめている**のに、受け口（`ondragover` / `ondrop` / `shkOver` など）だけ残っていて、
  読む人に「2通りある」と勘違いさせていた。**画面の属性も関数も全部消した。**
- 🔴 **この画面のドラッグは「ポインタ方式」の1本だけ。** 標準のドラッグに戻すと
  **タブレットで帯が動かせなくなる**（それが v0.124.1 で変えた理由）。復活させないこと。

### そのまま残したもの（v1.123.0 / v1.124.0 のぶん）
- 決定チップの拡張は**必ず1行**（折り返すと下の行が画面外へ押し出される）。
- 帯を掴んだ瞬間に**決定の行を画面の中へ出す**（端の帯より下に置く）。
- ドラッグ中の**自動スクロール**（縦＝画面／横＝表）＋暴走よけ。

---

## 🗂 触ったところ

| どこ | 何 |
|---|---|
| `pitflow\js\shaken.js` | 決定の行に `shk-decrow` の印。`ondrag*` の属性と `shkDragStart`/`shkOver`/`shkGanttOver` など**8個の関数を削除**。ドラッグの説明を書き直し |
| `pitflow\css\shaken.css` | `.shk-decrow .shk-sc` / `.shk-decrow .shk-decell` で枠を箱いっぱいに。ゴーストを `absolute` に |
| `pitflow\index.html` | 版3か所を **v1.125.0**、`shaken.js?v=28`・`shaken.css?v=20` |
| `pitflow\test_shaken_ops.mjs` | 見張り **+8本**（42 OK） |

---

## ✅ 確かめたこと

`test_shaken_ops.mjs` ＝ **42 OK / 0 NG**（+8本・1366×768）
- 🔴 **空の枠が箱いっぱいに広がる**（背の高い行でも縮まない・306px）
- 🔴 **カードの無いところ（枠の下のほう）でも光る／そこで離すと決まる**
- ゴーストは浮いていて表を伸び縮みさせない
- 🔴 標準のドラッグの受け口が**関数も属性も1つも残っていない**

**本物のマウスで動かす試験**でも、5台入って背高になった行の
**空いている午後の枠のいちばん下**へ、3台とも落とせることを確認した。
ほかも緑＝`test_shaken_unsched` 48 ／ `test_shaken_decide` 33 ／ `test_shaken_detail` 25 ／
`test_order_wash` 28 ／ `test_shaken_share` 27。

⚠ **タッチ（タブレット）では試していない。**

---

## 🚀 出しかた

```powershell
powershell -File "D:\Claude\アプリ開発\deploy-pitflow.ps1" -Message "v1.125.0 車検予定＝落とす枠をその日の午前／午後の箱いっぱいに（ドラッグの作りも1本に整理）"
```

⚠ `_shared` と `firestore.rules` は触っていない。MHS・CarFlow も触っていない。

### 📣 お知らせ
**足していない**（不具合の手当てなので）。

---

## ⏭ 次にやること

1. 🔴 **落とす場所は「当たり判定が見た目どおりか」を必ず見る。**
   今回は3回作り直した。原因はぜんぶ「見えている枠と、当たり判定の大きさが違う」だった。
2. 済にする前の予約詳細にも陸運局とRを出すか／MHS・前日LINEにも出すか。
3. 🔴 未入庫（来なかった車）になると車検予定から黙って消える（v1.115.0 から持ち越し・要確認）
