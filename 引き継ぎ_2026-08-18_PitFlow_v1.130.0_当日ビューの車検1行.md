# 引き継ぎ 2026-08-18 ／ PitFlow **v1.130.0**（＋ MHS **v1.31.0**）
## 当日ビューと MHS の当日に「**車検 ○○様 車種 下4桁 担当 陸運局 R**」の1行を出す

> 🗣 ゆうた
> 「**車検予定の　車検　客名　車種　ナンバーの下４桁　車検担当者　陸運局　R　の情報を
> 　当日ビューや MHS の当日に表示できるように修正**」
> 「現状車検枠で『車検』だけだが、**ゆくゆくは名変とかも入るようにしたいため頭に車検を付けてほしい**」

---

## 🎯 何をしたか（ひとことで）

朝の現場が見る **PitFlow の当日ビュー**と、工場に出しっぱなしの **MHS の当日**に、
その日の車検が**1台1行**で並ぶようにした。

```
車検  AM  田中様  ハスラー  1234  山田  野田  2R
車検  AM  済  鈴木様  N-BOX  3333  鈴木  千葉  4R
車検  PM  佐藤様  ゴルフ  0078  未定 回送・陸運局・R
```

出す順は **車検（種類）→ AM/PM →（済の印）→ 客名 → 車種 → ナンバー下4桁 → 担当 → 陸運局 → R**。
**PitFlow と MHS で順も中身も同じ。**

---

## 🏷 頭の「車検」は**枠の名前ではなく1行ごとの種類**

> 🔴 ここが今回いちばん大事な設計。
> 「車検」は**その行が何の用事か**を表す札。**枠の見出しではない。**
> ゆくゆく **名変**・**構造変更**・**予備検** などが同じ枠に混ざる前提で、
> 行ごとに種類を持たせてある。

- 種類は**物差し（`pit-share.js`）が配る**。`row.kind`。
- いまは `PIT_SHAKEN_KIND = '車検'` の1種類だけなので、全部の行が「車検」になる。
- **名変を足すときは `pit-share.js` の1か所（`kind` の決め方）を直すだけ**で、
  PitFlow の当日ビューも MHS の当日も**同時に**変わる。画面側は触らなくていい。

⚠ **画面の中に '車検' という字を直接書かないこと。** 書くと2か所直す羽目になる。

---

## 📏 物差しは1本（`js/pit-share.js`）

その日どの車を出すか・並び・中身は、**前からある `pitShakenOnDate(cards, iso)` 1本**。
今回そこに **2つ足した**だけで、当日ビューも MHS も同じものを見ている。

### ① `PIT_SHAKEN_KIND`（用事の種類）
```js
w.PIT_SHAKEN_KIND = '車検';   // ゆくゆく名変などが増える
```

### ② `pitPlate4(c)`（ナンバーの下4桁）
```js
pitPlate4({plate:'野田 500 あ 12-34'})   // → '1234'
pitPlate4({plate:'習志野 330 ね 0078'})  // → '0078'   ← 頭の0を落とさない
pitPlate4({plate:''})                    // → ''       ← 無ければ空。作らない
```
- 全角数字は半角に直してから、**数字以外を全部落として、後ろから4つ**。
- 🔴 **文字列のまま扱う。** 数にすると `0078` が `78` になって別の車になる。
- 🔴 **ナンバーが入っていなければ空。** 「----」などを勝手に置かない（2026-08-13 の決めごと）。

### `pitShakenOnDate` が配る1行
```js
{ id, card, state, mark, done, slot,
  kind,      // ← 追加。'車検'
  plate4,    // ← 追加。'1234'
  name, car, staff, office, round, div, divColor }
```
- `staff` ＝ **通称＆苗字**（v1.127.0）／ `office` ＝ **地名だけ**（v1.129.0）。
  狭い枠だから。**カード詳細とホバーだけがフルネーム・正式名。**

⚠ `pit-share.js` は **MHS が動いている本番の PitFlow から借りている**。
　→ **PitFlow を先に出す。MHS はそのあと。** 逆にすると MHS は自前の古い写しに落ちて「読み込み中」になる。

---

## 🖥 PitFlow 側（`js/today.js` ／ `css/views.css`）

- `_todShakenHtml(dayStr)` を足して、**入庫・返車の2列の下**に `.today-shk` の帯を出す。
- 見出し＝**「車検 3　AM 2・PM 1」**（台数と午前午後の内訳）。
- 行をクリック＝**そのカードの詳細が開く**（他の当日ビューと同じ動き）。
- 決まっていないもの（担当・陸運局・R）は、バラバラに「未定」を3つ出さず
  **「未定 回送・陸運局・R」の1つにまとめる**。狭いから。
- 🔴 **済んだ車には「未定」を出さない。** 終わった車の未定は意味がない。
- 🔴 **0台の日も枠は出す**（「車検予定なし」）。黙って消えると、
  **予定が無いのか壊れているのか現場に分からない。**

---

## 📺 MHS 側（`MHS\index.html` ／ v1.31.0）

- `pitShakenHTML` の1行を PitFlow と**同じ順・同じ中身**に作り直した。
- 借りるものリスト（`pit-share.js` から取る名前）に
  `pitPlate4` `pitStaffCall` `pitStaffFull` `pitLocShort` `pitShakenOfficeShort` を足した。
  ⚠ このリストは **全部そろっていないと丸ごと自前の写しに落ちる**作り。名前を1つ書き忘れると静かに古い表示になる。
- 横に伸びるので `.bshk` を**折り返し可**にし、縦フルビュー（`--pv-shk`）でも字が潰れない大きさにした。

---

## ✅ 見張り（試験）

| ファイル | 中身 | 結果 |
|---|---|---|
| **`test_today_shaken.mjs`（新）** | 下4桁の物差し／枠が列の下／7つの中身／未定のまとめ／済に未定を出さない／0台でも枠／物差しと画面の一致 | **27 OK** |
| `test_shaken_detail.mjs` | 予約詳細の車検 | 41 OK |
| `test_shaken_decide.mjs` | 決定の窓（担当・陸運局・R） | 36 OK |
| `test_shaken_ops.mjs` | クリックとドラッグ | 54 OK |
| `test_shaken_unsched.mjs` | 未入庫の3つの箱 | 48 OK |
| `test_order_wash.mjs` | 受注のときの洗車 | 28 OK |
| `test_shaken_share.mjs` | 物差し1本 | 27 OK |

```
python3 -m http.server 8983     ← 別ウィンドウ
node test_today_shaken.mjs
```

---

## ⏭ 次に考えること（頼まれていない。相談してから）

- **名変・構造変更などの種類を実際に足す**とき＝`pit-share.js` の `kind` の決め方1か所。
  そのとき、**どの作業種別（`workTypes`）を何と呼ぶか**をゆうたに決めてもらう。
- 前日LINEの画像にも下4桁・陸運局・R を出すか（いまは出していない。**画像は自動試験が無い**ので要注意）。
- `mydash.js` の車検BOXは今回**触っていない**。同じ1行にそろえるか。
- 済にする前の予約詳細にも陸運局とRを出すか（v1.120.0 からの持ち越し）。
- 未入庫（来なかった車）が車検予定から黙って消える件（v1.115.0 からの持ち越し）。

---

## 📌 触ったファイル

```
PitFlow\pitflow\js\pit-share.js     ← PIT_SHAKEN_KIND / pitPlate4 / 行に kind・plate4
PitFlow\pitflow\js\today.js         ← _todShakenHtml()
PitFlow\pitflow\css\views.css       ← .today-shk / .tshk-*
PitFlow\pitflow\index.html          ← v1.130.0（meta・ログイン・トップバーの3か所）＋ ?v= 更新
PitFlow\pitflow\test_today_shaken.mjs（新）
MHS\index.html                      ← v1.31.0
```
