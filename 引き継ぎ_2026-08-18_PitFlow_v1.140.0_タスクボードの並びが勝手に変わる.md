# 引き継ぎ 2026-08-18 ─ PitFlow **v1.140.0**
# タスクボードの並びが勝手に変わる／一時並び替え／メンバー絞り込み

> 🗣 ゆうた「タスクビューのバーの位置が勝手に変わる。というか**自動で並び替えが入る**。それを修正したい。
> 　　あくまで**人が動かした順をマスター並び**として、その状態から動かないようにしたい。
> 　　それと同時に**一時並び替え機能**を追加。入庫日が早い順／代車リミット近い順／暫定含め金額が大きい順。
> 　　**マスター並びは普遍的に変わらない。**
> 　　また**自分以外のメンバー（個人個人）だけでソートする機能**も搭載する。**ボタンは担当車両とは分ける**」

---

## 🔴 いちばん大事なこと ─ 原因は「並び替えが入っていた」ではない

**並び順が、どこにも保存されていなかった。**

看板の列は `state.cards` という **1本の配列の順**をそのまま出しているだけだった。
カードを掴んで動かすと、その配列の中で入れ替える（`dnd.js` の `reorder`）**だけ**で、
**カードのデータには何も書いていない**＝クラウドには1バイトも送られていなかった。

だからこうなっていた。

| 起きていたこと | 理由 |
|---|---|
| 開き直すと元に戻る | 読み込み直後の並びは Firestore が返す順（＝ほぼ**ドキュメントID順**） |
| 別の人の端末では違う順に見える | 動かした順は**その人の画面の中だけ**の話だった |
| 新しい予約が末尾に付く／他の人が直すと入れ替わる | 購読で届いたカードは配列の末尾に `push` される |
| 自動更新（v1.117.0）のたびに戻る | 読み直すと同じことが起きる |

🔴 **「勝手に並び替わる」のではなく、「人が決めた順を覚えていなかった」。**
　 直しは「並び替えを止める」ではなく「**並びを覚えさせる**」だった。

---

## 🗺 作りの地図（次に触る人へ）

| どこ | 何がある |
|---|---|
| `js/board-order.js` 🆕 | 🔴 **マスター並びの物差し1本**＝`boardOrder`（並び番号）。<br>`sort` / `moveBefore` / `moveToEnd` / `ensure` / `onBoard`。<br>**`boardOrder` を書くのはこのファイルだけ。** ほかで直接いじらない |
| `js/board-sort.js` 🆕 | 一時並び替え。3つの物差し（入庫日・代車リミット・金額）と、青い帯・数字バッジ・プルダウン |
| `js/myonly-pit.js` | 「担当車両」＋🆕「メンバー」。**誰で絞るかの物差しは1本**（`passFor`）。緑の帯 |
| `js/task.js` | 列を描く。並びは①マスター並び→②一時並び替え の2段。帯は `.kanban` の**外**に出す |
| `js/dnd.js` | 掴んで落とす。**reorder で `PitBoardOrder.moveBefore`**／工程移動で `moveToEnd`／並び替え中は掴めない |
| `js/board-line.js` | 区切りライン。並び替え中は**薄く出したまま・掴めない**（位置は「上から何枚目」を守る） |

### 保存するもの（増えたのは1つだけ）

```
カード.boardOrder … その列の中での位置。数字（10, 20, 30 … と間を空ける）
```

`pitCards` の1フィールドなので、`PitDB.save()` の差分保存にそのまま乗る。**新しい入れ物は作っていない。**

---

## 🧷 決めごと（この5つを崩さないこと）

| | ルール |
|---|---|
| **S1** | 🔴 **番号が変わるのは「人がカードを掴んで落とした時」だけ。**<br>読み込み・自動更新・他の人の編集では**1ミリも動かない** |
| **S2** | 🔴 **番号を書く所は `board-order.js` だけ。** ほかのファイルで `boardOrder` を読み書きしない |
| **S3** | 番号を持っていないカード（新しく来た・工程を移した）は **列のいちばん下**。黙って割り込ませない |
| **S4** | 🔴 **一時並び替えはデータを1バイトも触らない。**保存しない・ビューを移ると解除（担当車両と同じ考え方） |
| **S5** | 🔴 **並び替え中はカードも区切りラインも動かせない。**仮の並びのまま掴むとマスター並びが壊れるため |

### 番号を振る範囲を広げないこと（S2 の裏側）

`ensure()` が番号を振るのは **いま盤面に乗っているカードだけ**（`onBoard`）。
返車済み・アーカイブ・下書きには振らない。
⚠ 全カードに振ると**何千件ものクラウド書き込みが一度に飛ぶ**。範囲を広げる時は理由を書くこと。

---

## 🎛 画面（タスクボード右上）

```
  [担当車両] [👤 メンバー] [⇅ 並び替え] [区切りライン] [完TEL済] [完TEL依頼] [PITボード]
```

| ボタン | 誰に出る | すること |
|---|---|---|
| **担当車両** | フロントの人 | いままでどおり。**自分**の担当だけ／1課2課をまたいで集める |
| **👤 メンバー** 🆕 | 全員 | **選んだ1人**の担当だけ残す。プルダウンで選ぶ（台数つき） |
| **⇅ 並び替え** 🆕 | 全員 | マスター並び／入庫日が早い順／代車リミットが近い順／金額が大きい順 |

- 🔴 **担当車両とメンバーは同時に効かない**（ゆうた 2026-08-18「同時に効かせたい場面はない」）。片方を選ぶともう片方が外れる
- **絞り込み＝緑の帯／一時並び替え＝青の帯**。色を分けているのは
  「いま並びが変なのは**並び替えのせい**か**絞り込みのせい**か」を一目で切り分けるため。**片方だけ色を変えないこと**
- 絞り込みと並び替えは**同時に効く**（帯が2本出る）

### 3つの物差し

| 並び | 何で並ぶか | 決めごと |
|---|---|---|
| **入庫日が早い順** | `reserveDate` の古い順 | 同じ日は**入庫時刻**の早い順。日付なしは**いちばん下** |
| **代車リミットが近い順** | 残り日数 | 🔴 **超過（マイナス）がいちばん上**。代車なしは**いちばん下**。<br>物差しは `pitLoanerRemainOf`（loaner-free.js の1本）を**借りるだけ**＝ここで引き算しない |
| **金額が大きい順**<br>（暫定含め） | 確定 → 受注 → 見積 → 概算 | 無ければ作業タイプの概算（`pitEstAmount`）。<br>🔴 **どの段の金額かをバッジに出す**（確/受/見/概）＝概算だけの車が上に来た時に分かる（ゆうた指定） |

カード右上の青いバッジに、その物差しの値がそのまま出る（`8/12 09:00` ／ `超過3日` ／ `受 ¥210,000`）。
**何順で並んでいるかを数字で確かめられる。**

---

## ✅ テスト（全部緑）

| テスト | 件数 | 主に見張っているもの |
|---|---|---|
| `test_board_order.mjs` 🆕 | **31 OK** | 🔴 **配列の順が逆さになっても画面の並びが変わらない**／人が動かした順が残る／新しいカードは末尾／並び替えでデータが変わらない／並び替え中は掴めない／メンバーと担当車両が同時に効かない |
| `test_nosale.mjs` | 150 OK | 回帰（⋮ 全ステージ） |
| `test_disp3.mjs` | 70 OK | 回帰 |
| `test_done_flow.mjs` | 60 OK | 回帰 |
| `test_ctxmenu.mjs` | 56 OK | 右クリック |
| `test_board_line.mjs` | 53 OK | 区切りライン |
| `test_return_gate.mjs` | 44 OK | 完TELの3択 |
| `test_myonly_cross.mjs` | 35 OK | 課をまたぐ担当車両（**配線チェックを1行更新**） |
| `test_mech_chip.mjs` 34 ／ `test_myonly` 29 ／ `test_card_scope` 29 ／ `test_today_div` 25 ／ `test_line_myonly` 25 ／ `test_time_views` 24 | | 回帰 |

⚠ `test_myonly_cross.mjs` の「カードの印付けも1か所を通している」だけ**中身を書き換えた**。
　 `_card` に一時並び替えのバッジをつないだので1行→数行になったため。
　 **見張っているもの（通り道が1本であること）は同じ**＝`PitMyOnly.decorate` も `PitBoardSort.decorate` も
　 呼ばれるのは1か所だけ、という数え方に変えてある。

---

## 📄 資料

| ファイル | 中身 |
|---|---|
| `PitFlow\モック_タスクビューの並び順_2026-08-18.html` | **触れるモック**（原因の説明・ボタン配置・ドラッグと並び替えの実演・決めごと案）。**合意ずみ** |

---

## 次にやること

### 1. 初回の番号ふりを本番で確かめる
ゆうた指定＝「**とりあえずいまの並びのまま振って**」。
本番で最初に開いた人の画面で `[board-order] 並び番号を N 枚に振りました` が出て、そのまま保存される。
⚠ 読み込み直後の並びは**ドキュメントID順＝どの端末でも同じ**なので、誰が最初に開いても同じ番号になる。
👉 **1回振ったあとに「思っていた並びと違う」となったら、その時は手で並べ直してもらう**（そこからは崩れない）。

### 2. 気づいているが手を付けていないもの

- **`archived` をカードに直接書いている所が2つある**（`cvCancelResv` と `pitAutoArchive`）＝v1.139.0 からの持ち越し
- **メンバーのプルダウンに出るのは「フロント」にチェックがある人だけ。**
  メカで絞りたい要望が出たら、`passFor` に「どの担当で見るか」を足す形になる（いまは物差し1本＝フロント担当）
- **一時並び替えは盤面ぜんぶの列に一括**（ゆうた選択）。列ごとに変えたい要望が出たら列の見出しに小さく付ける形

---

## デプロイ

```
powershell -File "D:\Claude\アプリ開発\deploy-pitflow.ps1" -Message "v1.140.0 タスクボードの並びを覚える＝人が動かした順をマスター並びとして保存（boardOrder）／一時並び替え3種（入庫日・代車リミット・金額）／メンバー絞り込みを担当車両と別ボタンで追加"
```
