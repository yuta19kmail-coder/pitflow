# 引き継ぎ 2026-08-18 ── 🚙 決めたら「回送の担当・陸運局・R」を入れる（PitFlow **v1.119.0**）

> 🔴 **ゆうたはコードを読まない。** 話すときはファイル名・関数名・行番号を出さない。
> ⚠ 同じ日の続き。v1.115.0 → v1.116.0/117 → v1.118.0 の書き置きを先に読むこと。

---

## 🗣 依頼

> 「決定車両になった時点で **担当者と どこ陸運局か 何Rか** を入力できるように。**R はラウンドで1〜4** がある。
> 　**陸運局の情報はコアメンバーズの場所から陸運支局バッジが付いているもの**を引っ張ってくるようにしたい。
> 　そしてそれらの情報を**決定した車両カードの下などに拡張で**付けたい」
> 追記 🗣「**担当者というのは実際に車検に行く、回送の担当者ね**」

## ✅ ゆうたに確かめた3つ（この通りに作った）

| 聞いたこと | 答え |
|---|---|
| 入れるタイミング | **決めた瞬間に窓**。日を動かしただけの時は出さない。あとからチップを押しても直せる |
| 担当の入れ物 | 今までの「陸運局へ行った人」と**同じ欄**にする |
| 陸運局・Rが空 | **空でも決定できる。ただしチップに「未定」の印を出す** |

---

## 🔴🔴 決めごと

### 担当＝**回送の担当**。入れ物は今までと同じ1つ
- `inspSchedule.resultStaff` に入れる。**新しい欄は作っていない。**
- ＝ **決めた時点で入れると、MHS の当日ビューと前日LINEの画像にも前もって名前が出る。**
  🔴 これは今までと変わった点。前は「済」を記録するまで空だった。**現場に先に知らせたい時は今すぐ効く。**
- ⚠ **受付のフロント担当とは別物**（2026-08-16 の決めごとのまま）。混ぜない。

### 陸運局＝**CoreMembers の場所マスター**が正。PitFlow では作れない
- 拾う条件＝場所のカテゴリが **`rikuun`（陸運局）**。
  ⚠ CoreMembers 側でカテゴリ名を作り直せるので、**鍵が `rikuun` か、名札に「陸運」が入っていれば拾う**（取りこぼさない側）。
- 「有効」が外れている場所は出さない。並びは **よく使う → 並び順 → 名前**（CoreMembers と同じ考え方）。
- 窓口は `members-pit.js` の `pitRikuunList()` / `pitLocName()` **1本**。ここ以外で場所を読まないこと。
- 持ち方＝`inspSchedule.office`（**id**）＋ `officeName`（**名前の写し**）。
  ⚠ 出す時は**本家（CoreMembers）が優先**。写しは「場所が消された・名前が変わった」時に
     過去の記録が空欄にならないための後ろ盾**だけ**。表示の元にしないこと。
- ⚠ **サンプル・デモ版は CoreMembers を読まない**ので、見本の陸運局を3件だけ内蔵した（本番では使われない）。

### R（ラウンド）
- `inspSchedule.round` に **1〜4 の数字**。空・0＝未定。

### 見え方（ゆうた指定「決定した車両カードの下などに拡張で」）
- 決定チップの下に **回送の担当（緑）／陸運局（青）／R（黄）** の小さいバッジ。
- 🔴 **「未定」の印を出すのは、これから行く車（決定）だけ。** 済・再検には出さない
  （終わった話に「未定」と出しても直しようがない）。
- ⚠ 枠が狭い（1枠118px）ので長い名前は切れる。**吹き出しに全部入れてある。**

### 消える・残るの線引き
- 日を動かしても・再検にしても **陸運局とRは残す**（たいてい同じ所へ行くので入れ直させない）。
- 再検にすると担当は空に戻る（今までどおり）。**その時の担当・陸運局・Rは再検の記録に残す。**

---

## 🗂 触ったところ

| どこ | 何 |
|---|---|
| `pitflow\js\members-pit.js` | **場所マスター（`coreLocations`）の購読を追加**。`pitIsRikuunLoc` / `pitRikuunList` / `pitLocName` を新設 |
| `pitflow\js\pit-share.js` | `pitShakenOffice` / `pitShakenRound` / `PIT_SHAKEN_ROUNDS` を新設。`pitShakenOnDate` の戻りに **`office` と `round` を追加**（MHS・LINEが使いたくなった時に条件を写さないで済むように） |
| `pitflow\js\shaken.js` | `fieldsHtml`（3つの入力欄）・`shkDecidePop`（決めた直後の窓）・`shkSaveFields`（保存）・`chipMeta`（チップの下の拡張）を新設。`assign` は**はじめて決めた時だけ**窓を出す。完了・再検でも3つを一緒に確定 |
| `pitflow\css\shaken.css` | `.shk-meta` / `.shk-mt`（st・of・rd・tbd）/ `.shk-phint` / `.shk-pbtn.ok2` |
| `pitflow\js\help-content.js` | 車検予定のヘルプに3つの説明を追加 |
| `pitflow\index.html` | 版3か所を **v1.119.0**、`shaken.js?v=24`・`shaken.css?v=17`・`pit-share.js?v=8`・`members-pit.js?v=16`・`help-content.js?v=28` |
| `pitflow\test_shaken_decide.mjs` | **新設**（見張り） |

⚠ **`firestore.rules` は触っていない。** `coreLocations` は会社メンバーなら読める設定に元からなっている（CarFlow のルールが全アプリ共通）。

---

## ✅ 確かめたこと

`pitflow\test_shaken_decide.mjs`（新設）＝ **33 OK / 0 NG**

- 陸運局は**陸運局のバッジだけ拾う**（部品商・無効な場所・関係ないカテゴリは拾わない）
- 決めた瞬間に窓が開く／担当・陸運局・R が並ぶ／R は 1R〜4R＋（未定）
- 🔴 決めた直後の窓に**「完了」ボタンは出さない**（押し間違いで済にしないため）
- 保存すると**チップの下に3つとも出る**／フローに1行残る
- **同じ物差し（pitShakenOnDate）から担当・陸運局・R が引ける**＝MHSとLINEも同じ答え
- 空なら「回送 未定／陸運局 未定／R未定」／**済んだ車には未定を出さない**
- 🔴 **日を動かしただけの時は窓を出さない**／担当・陸運局・R は消えない
- あとからチップを押して直せる（そのメニューには完了・再検も今までどおり）

既存も緑のまま＝`test_shaken_ops` 21 OK ／ `test_shaken_unsched` 43 OK ／ `test_shaken_share` 27 OK。

```
python3 -m http.server 8977    ← 別ウィンドウ
node test_shaken_decide.mjs
```

⚠ **本番の CoreMembers に「陸運局」の場所が登録されていないと、陸運局は選べない**（窓に案内文が出る）。
　 出す前に CoreMembers の場所マスターを見ておくこと。

---

## 🚀 出しかた

```powershell
powershell -File "D:\Claude\アプリ開発\deploy-pitflow.ps1" -Message "v1.119.0 車検を決めたら回送の担当・陸運局・Rを入れられる（陸運局はCoreMembersの場所から）"
```

⚠ `_shared` と `firestore.rules` は触っていない。MHS・CarFlow も触っていない。
⚠ ただし **`pit-share.js` は MHS も借りている**。今回は足しただけ（`office`・`round`）なので MHS の動きは変わらない。

### 📣 お知らせ
🔴 **足すのを勧める。** 新しく入れる欄が3つ増え、**担当を決めた時点で入れると前日LINEの画像にも名前が出る**ようになるため。

---

## ⏭ 次にやること

1. MHS の当日ビュー／前日LINEの画像に**陸運局とRも出すか**（物差しからは引ける状態にしてある）。ゆうたに聞いてから。
2. 🔴 **未入庫（来なかった車）になると、車検予定から黙って消える**（v1.115.0 から持ち越し・要確認）
3. 前からの持ち越し（`mydash.js` の車検BOX／予約詳細の「陸運局休」／LINE画像の自動試験なし）
