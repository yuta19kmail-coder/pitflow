# 引き継ぎ 2026-08-18 ─ PitFlow **v1.133.0 〜 v1.139.0**
# 右クリックと ⋮ の精査（カードの一生・全ステージ）

> 🗣 ゆうた「各ビューの右クリックとカード詳細の…の内容を精査した。
> 　　今不必要に入ってたり、**逆に間違えた時に戻せないなど不具合が多い**。
> 　　流れを壊す可能性もあるので、まずは精査して、**一定のルールを持って**やりたい」
> 🗣 「**場面、ステージごとに見ていきたい**」

**カードの一生を①〜⑧のステージに割って、1ステージずつ「いま／直したあと」のモックを出し、
OKが出たぶんだけ実装する**、という進め方をした。7版。**カード側は全ステージ終わり。**

---

## 🔴 このセッションでいちばん大事なもの＝決まったルール

次に触る人は、**まずこれだけ読めばいい。**

| | ルール |
|---|---|
| **R1** | **右クリック＝近道だけ。状態は変えない。**「開く・見る・写す・刷る」だけを載せる |
| **R2** | **⋮＝いまの状態から出る道だけ。**工程移動は載せない（v1.99.0 の決着） |
| **R3** | 🔴 **入口を作ったら、出口も同時に作る。片道を作らない。**<br>片道にしかできない操作は、**その場で「戻せません」と言い切る** |
| **R4** | **「消す」ではなく「アーカイブ」に寄せる。**実体削除は最後の手段 |
| **R5** | 🔴 **窓に書いた戻し方は、実行後に本当にメニューへ出ていること。**テストで見張る |
| **R6** | **重い操作は下・区切り線の下。赤は1つだけ**（＝消去する） |
| **R7** | **同じ状態は同じ言葉と同じ印で表す。**物差しを2か所に書かない |
| **R8** | 🔴 **アーカイブ済みの車は「見るだけ」。アーカイブから戻す・数字を直すのは管理者だけ。**<br>物差しは `pitIsAdmin()`（マスター＋管理）。**マスター1人には絞らない**（ゆうたが居ない日に誰も直せなくなる）<br>練習用（`!PIT_CLOUD`）は今までどおり全部さわれる |

### 🔴 R8 の見本は「発明していない」

**顧客・車両のアーカイブ（`archive-pit.js`）で、ゆうたが既にOKを出した形をそのまま持ってきた。**

- アーカイブする＝誰でも／**アーカイブから戻す＝管理者だけ**
- アーカイブ済みは**上に帯**が出る（開いた瞬間に分かる）
- 押せない人には **🔒 管理のみ** の札。押すと「**戻せるのは管理者だけです**」の窓
- **黙って消さない**

---

## 🗣 言葉の決めごと（v1.135.0・v1.136.0 でそろえた）

| 言葉 | 意味 | 誰が |
|---|---|---|
| **アーカイブ** | 終わった／もう動かさない状態。**データは消えない** | — |
| **アーカイブする** | その状態にする | **誰でも** |
| **アーカイブから戻す** | 元に戻す | **管理者だけ** |
| **消去する** | 本当に無くなる。**戻せない** | 誰でも（ただし**2枚聞く**） |
| **実績化** | 完TELからその場で返車済みにして確定売上に固める（v1.97.0） | — |

🔴 **「片付ける」は画面から全部消した。**「削除」も「消す」も使わない＝**「消去」1語**。
⚠ 顧客・車両の「アーカイブ」とカードの「アーカイブ」は**同じ言葉でよい**（同じ考え方だから）。
⚠ 設定の「空の予約カードを消去する」だけは**本当に消える**ので、アーカイブとは呼ばない。

---

## 📋 最終形 ── カードの一生 × ⋮ の早見表

| ステージ | ⋮ に出るもの |
|---|---|
| **① 予約（ふつう）** | 仮予約にする／承認予約にする／入庫中にする／予約キャンセルにする…／**消去する…** |
| **① 予約（仮予約）** | ✓本予約に確定する／承認予約にする／入庫中にする／予約キャンセルにする…／**消去する…** |
| **① 予約（承認待ち）** | **↩承認に回すのをやめる…**／入庫中にする／予約キャンセルにする…／**消去する…** |
| **② タスクボードの上** | 入庫を取り消して予約に戻す…／売上なしでアーカイブする…／**消去する…** |
| **③ 完TELを通った** | **タスクボードに戻す…**／売上なしでアーカイブする…／**消去する…** |
| **④ 実績（返車済み）** | 管理者＝**アーカイブから戻す…**（→返車前）／それ以外＝🔒管理のみ　＋**消去する…** |
| **⑤ 売上なし** | 管理者＝**アーカイブから戻す…**（→タスクボード）／それ以外＝🔒管理のみ　＋**消去する…** |
| **⑥ 未入庫（30日以内）** | **消去する…** だけ（戻すのは予約▸未定の未入庫BOXから・誰でも） |
| **⑥ 未入庫（30日後）** | 管理者＝**アーカイブから戻す…**（→予約・日付を選ぶ）／それ以外＝🔒管理のみ　＋**消去する…** |
| **⑦ 予約キャンセル** | 管理者＝**アーカイブから戻す…**（→予約・日付を選ぶ）／それ以外＝🔒管理のみ　＋**消去する…** |
| **⑧ 廃車・乗替** | 入庫を取り消して予約に戻す…／売上なしでアーカイブする…／**消去する…** |

**右クリックは全ステージ共通で**「詳細を開く／別タブで開く／表紙を印刷／コピー▸」＋
・予約だけ「入庫済みにする」
・**タスク看板を見ている時だけ**「この下にラインを入れる」

---

## 🗂 版ごとの中身

### v1.133.0 右クリックの「この下にラインを入れる」をタスク看板だけに
> 🗣「右クリメニューも『ラインを引く』とかタスクビューでしか使えないのとかでてるよ」

- 条件が「カードの状態」だけだったので、**当日・返車・PIT配置図・駐車場・代車カレンダー・検索**でも出ていた。
  押しても線はその画面に出ない＝何も起きていないように見える。
- 🔴 出す条件を **`board-line.js` の `ctxItem` 1本**に移した（`ctxmenu-pit.js` には条件を書かない）。
  ① タスク看板（`task`／`course1`／`course2`）　② カードが盤面に乗っている（`!returnStage` ほか）

### v1.134.0 承認待ちの ⋮ を整理／「承認に回すのをやめる」
- `cvToggleTentative` は `tentative` を裏返すだけで `approvalPending` を触っていなかった。
  逆向きは `tentative=false` にしてあったので**片方だけ対策されていた**
  ＝承認待ちの車で「仮予約にする」を押すと**札が2つ並ぶ**（v1.74.0 の決めごと違反）。
- 🔴 承認待ちには「仮予約にする」を出さない。代わりに **「↩ 承認に回すのをやめる」**（入口に出口をそろえる）。
  中身は `approval-pit.js` の **`pitUnapproveCard`**＝`pitApproveCard` の真下に置いた。
  ⚠ フローの言葉を変えてある（「承認した」／「承認に回すのをやめた」）＝**あとから見分けられる**。

### v1.135.0 言葉そろえ（「片付ける」を画面から消した）

### v1.136.0 アーカイブの形を作った（R8 の実装）
- `archive-pit.js` に **`cardArchived` / `cardArchiveNote` / `denyRestore`** を新設（顧客・車両と同じ場所）。
- カード詳細の**いちばん上に帯**。⋮ は管理者だけ「アーカイブから戻す」。
- 🔴 **消去は2枚聞く**（ゆうた指定「消すは誰でもでいいが、ポップアップを2重で出す」）。
  1枚目＝戻せないこと／**ふつうはアーカイブに落ち着くこと**　2枚目＝車を名指しした最終確認。
- 「戻せるのは管理者だけです」の窓を `archive-pit.js` の1本に寄せ、`customers.js` はそれを呼ぶだけに
  （エラー番号 PF-0020 が2か所から出てテストが落ちて気づいた）。

### v1.137.0 完TELを通った車は3択／返車済みに確認1枚
- 入る道はドラッグ1つなのに、出る道が「予約まで全部戻す」しかなかった。
  🔴 **「タスクボードに戻す」**＝`returnStage` **だけ**消す。工程・返車予定・確定金額は残す。
  売上の区分も動かない（完TEL中も作業完了済も同じ「確定」）。
- 🔴 **「返車済みにする」に確認の窓**。**いくらで固まるかを出す**＋「ここから先はアーカイブです。戻せるのは管理者だけです」。
  `pitTodayReturn`（聞く）と `_pitTodayReturnGo`（固める）に分離。関門は `_todReturnGate` の1本。

### v1.138.0 実績もアーカイブ扱い／確定売上は管理だけ
- 🔴 **確定売上金額が誰でも書き換えられた。**「🔒 確定」の札を出しておきながら、
  隣の「✏️ 編集」に権限の確認がどこにも無かった。表紙の ✏編集（概算・見積・受注・予定返車日）も同じ。
  → `canEditMoney()`（＝`canEditResultDate()` と同じ物差し）で締めた。**保存する所でも止めている。**
- 🔴 管理者は **アーカイブから戻す＝返車前（完TEL済）へ**（案B＝階段を1段だけ下りる）。
  消すのは `completedAt` だけ。**確定売上・返車日・入金日はそのまま**（ゆうた指定）。
  窓で**変わる月を名指し**する（案イ）。止めはしない＝決算前に直したい時に詰むため。

### v1.139.0 残り4つ（⑤⑥⑦⑧）
- **⑤ 売上なしの戻し先は「タスクボード」**（ゆうた「あるとしたら入れ間違えただけだと思う」）。
- **⑥⑦ 未入庫（30日後）・予約キャンセルもアーカイブ扱い**。管理者なら ⋮ から予約へ戻せる
  （戻す処理は未入庫BOXが呼んでいる **`pitUndRestore` を借りるだけ**＝写しを作らない）。
- **⑧ 廃車・乗替を `gone` から外した**＝タスクボードの側列にいるので、タスクボードの車と同じ3択。
- 🔴 **キャンセルの印を2種類に整理**＝当日ビューのキャンセルにも `noShow` を立てた。
  `noShow`＝来なかった（自動でも手動でも）／`cancelled`＝人が「やめます」と決めた。

---

## 🗺 作りの地図（次に触る人へ）

| どこ | 何がある |
|---|---|
| `js/archive-pit.js` | 🔴 **アーカイブの物差し1本**＝`cardArchived` / `cardIsResult` / `cardArchiveNote` / `canRestore` / `denyRestore`。<br>**新しい状態をアーカイブに入れたい時は、ここに1行足すだけで帯も ⋮ も権限も全部そろって効く** |
| `js/card-view.js` | ⋮ の中身＝`optMenuHtml` 1か所。金額の権限＝`canEditMoney`。帯＝`archBarHtml` |
| `js/ctxmenu-pit.js` | 右クリック（全ビュー共通）。**画面ごとに別メニューを作らない** |
| `js/board-line.js` | 区切りラインを**出す条件**（看板か／盤面に乗っているか） |
| `js/approval-pit.js` | 承認する／**承認に回すのをやめる**（2つ並べて置いてある） |
| `js/today.js` | 当日ビューのシート。返車の関門＝`_todReturnGate` 1本 |
| `js/undetermined.js` | 未入庫BOX／`pitUndRestore`（**⋮ からも借りている**） |

### 「戻す」は名前で2グループに分けてある（テストが本数を見張っている）

```
cvBackToReserve      … 入庫を取り消して予約に戻す   （アーカイブ前・誰でも）
cvBackToBoard        … 完TELを取り消してタスクボードへ（アーカイブ前・誰でも）
cvUnarchiveToBoard(Go)     … 売上なし → タスクボード （アーカイブから・管理者だけ）
cvUnarchiveToDelivery(Go)  … 実績     → 返車前       （アーカイブから・管理者だけ）
pitUndRestore(Go)          … 未入庫・予約キャンセル → 予約（undetermined.js を借りる）
```

🔴 **ボタンを消すだけにしない。** どれも**実行する関数の中でも同じ条件で止めている**
（v1.97.0 の返車の関門と同じやり方）。外から呼ばれても通らない。

---

## ✅ テスト（全部緑）

| テスト | 件数 | 主に見張っているもの |
|---|---|---|
| `test_nosale.mjs` | **150 OK** | ⋮ の出し分け全ステージ／権限／帯／消去2枚／戻し先／窓の文言と実物の一致 |
| `test_resv_detail.mjs` | 128 OK | 回帰 |
| `test_approval.mjs` | **81 OK** | 承認待ちの ⋮／承認と「やめる」の記録が混ざらないこと |
| `test_overdue.mjs` | 80 OK | 未入庫まわり |
| `test_loaner_free.mjs` | 79 OK | 代車 |
| `test_done_flow.mjs` | **60 OK** | 返車の関門／確認の窓 |
| `test_cover_edit.mjs` | 60 OK | 表紙の編集 |
| `test_save_menu.mjs` | 57 OK | その他保存 |
| `test_ctxmenu.mjs` | **56 OK** | 右クリック／ラインは看板だけ |
| `test_board_line.mjs` | 53 OK | 区切りライン |
| `test_result_date.mjs` | 45 OK | 実績カウント日 |
| `test_return_gate.mjs` | **44 OK** | 完TELの3択／タスクボードに戻す |
| `test_loaner_cal` 31 ／ `test_no_native_dialog` 28 ／ `test_sales_month` 23 ／ `test_errcode` 21 ／ `test_mydash_tbd` 15 | | 回帰 |

⚠ `test_custform.mjs` の6件は `顧客控え_ナンバー0を空にする.js` が**親フォルダ**にあるための環境差。今回の変更とは無関係。

---

## 📄 資料（`PitFlow\` 直下）

| ファイル | 中身 |
|---|---|
| `PitFlow_メニュー精査_2026-08-18.html` | **最初の全体精査**（現状の全一覧・不具合A〜H・ルール案・段取り） |
| `モック_メニュー_ステージ①予約_2026-08-18.html` | ①（4状態をタブで切替） |
| `モック_メニュー_ステージ②盤面_2026-08-18_v3.html` | ②＋**R8と言葉の決めごと**（v1・v2 は古い） |
| `モック_メニュー_ステージ③完TEL_2026-08-18.html` | ③ |
| `モック_メニュー_ステージ④実績_2026-08-18.html` | ④（**何を誰が触れるかの一覧**あり） |
| `モック_メニュー_ステージ⑤⑥⑦⑧_2026-08-18.html` | ⑤⑥⑦⑧（タブで切替・**キャンセルの印の表**あり） |

---

## 次にやること

### 1. カード以外の5つ（まだ手を付けていない）
①〜⑧と同じやり方（モック → すり合わせ → 実装）で進める。

| 対象 | いまの入口 | 気になっていること |
|---|---|---|
| **顧客** | 右クリック／顧客詳細の小アイコン | R8 の**見本**なので、たぶん直すところは無い |
| **車両** | 右クリック／車両詳細 | 削除が編集モーダルにある。アーカイブと混ざっていないか |
| **代車の貸出** | 右クリック／札のクリック | 🔴 **「この予約の代車をキャンセル」が実は削除**（説明は「カレンダーから外します」）。<br>🔴 **返却済みの札にも出る**＝「返却済みの貸出は消さない」という決めごとを手動メニューだけが破っている |
| **付箋** | 右クリック／⋮ | 済／未済／返信／消去。消去に確認が無い |
| **カレンダーの日** | 右クリック | 「この日で新規予約」が**入庫カードを書いている最中の空き予約カレンダー**でも出る（押すともう1枚増える） |

👉 **代車の貸出**がいちばん実害が大きい（貸した記録ごと消える）。次はここからがよさそう。

### 2. 気づいているが手を付けていない小さいもの

- **未入庫（30日以内）の ⋮ は「消去する」だけ。** 戻すのは未入庫BOXからだけ。
  ⋮ にも「予約に戻す」を出すか？（BOXにあるので急がない）
- **`_cvAsk(title, …)` の `title` は画面に出ていない。**
  `UI.confirm(title, opt)` は**第1引数が見出し**なので `opt.title` は上書きされる。
  直すと既存の窓の見出しが全部変わるので**触っていない**。見出しに出したい言葉は `msg` の1行目に書くこと。
- **`archived` をカードに直接書いている所が2つある**（`cvCancelResv` と `pitAutoArchive`）。
  `archive-pit.js` の「ここ以外で `archived` を読み書きしない」という注記とズレたまま。
  読む側は `cardArchived` に寄せたので実害は無いが、書く側もいつか寄せたい。

---

## デプロイ

```
powershell -File "D:\Claude\アプリ開発\deploy-pitflow.ps1" -Message "v1.139.0 メニュー精査 完了（v1.133.0〜v1.139.0）＝右クリックとカード詳細の⋮を全ステージ整理／アーカイブは管理者だけが戻せる／消去は2枚聞く／言葉をアーカイブ・消去で統一"
```
