# 引き継ぎ 2026-08-18 ── 🖱 車検予定の表：**予定は押すだけ／決定はドラッグだけ**（PitFlow **v1.118.0**）

> 🔴 **ゆうたはコードを読まない。** 話すときはファイル名・関数名・行番号を出さない。
> ⚠ 同じ日の続き。先に v1.115.0 → v1.116.0/v1.117.0 の書き置きを読むこと。

---

## 🗣 依頼

> 「表の中で
> 　**ドラッグの挙動は候補日を増やすのはなし。あくまで予定側の枠をクリックするのみ**
> 　逆に **決定車両への移動はドラッグのみ、予定部分をクリックで飛ばないように**
> 　また**ドラッグ時には既存のルール通りのカーソール表示**にする」

---

## 🔴🔴 これからの決めごと（表の中の操作は、この2本だけ）

> ### **予定（行ける枠）＝押す。決定＝ドラッグ。**
> **この2つを混ぜない。** 押して決定になる道は作らない／ドラッグで枠を塗る道も作らない。

| どこ | 何ができるか |
|---|---|
| 空いているマスを**押す** | その枠が「行ける枠」に入る |
| 帯（行ける枠）を**押す** | その枠が**外れる**（付け外しのトグル） |
| 帯を上の「**決定**」へ**ドラッグ** | その日・その枠で**決定**になる |
| 決定チップを**ドラッグ** | 別の日へ移す／「予定」へ落とすと候補に戻る |
| 決定チップを**押す** | 今までどおりメニュー（完了・再検・午前午後・取消） |

### なぜ
- 帯を押したら決定していた（`shkFix`）＝**押し間違いで陸運局の日が勝手に決まる**。
- 範囲ドラッグで枠を塗っていた（`shkPaint…`）＝**掴んだつもりが塗りになる**。
  表の中で掴む操作は「決定へ動かす」1つだけ、に寄せた。
- 🔴 **`shkFix` と `shkPaintStart` / `shkPaintMove` / `paintCommit` は削除した。復活させないこと。**
  代わりの窓口は `shkSlot(id, iso, slot)` 1本（押して入れる／押して外す）。

### カーソル
- 2026-08-16 の全アプリ共通の決めごとどおり **指さし（`cursor:pointer`）1本**。
- 枠のマスが `cursor:cell`（十字）だったのを**指さしに直した**。
  ⚠ `grab`／`grabbing` と同じ理由＝**Windows に標準の絵が無いカーソルは、環境によって黒フチが落ちて見えなくなる**。
- ドラッグ**している最中**のカーソルはブラウザとOSが描くので触れない（前からそう）。

### 🔴 ドラッグ中のカーソルについて（2026-08-18・ゆうた確認・**このままで確定**）

> 🗣 ゆうた「**ドラッグ中は矢印+箱のやつでは？**」

**他の画面はそのとおり。車検予定の表だけ違う。** 実際に測って確かめた：

| | ドラッグの仕組み | ドラッグ中のカーソル |
|---|---|---|
| タスクボード・代車・PIT配置図・付箋 など | ブラウザ標準（`draggable="true"`） | **矢印＋箱**（OSが描く） |
| **車検予定の表** | **ポインタ方式**（v0.124.1〜・`draggable="false"`） | 帯の上＝指さし／決定枠の上＝ただの矢印。**箱は出ない** |

- 車検予定だけポインタ方式なのは **タブレットのタッチで動かせるようにするため**（v0.124.1）。
  ブラウザ標準のドラッグに戻すと**タッチで帯を動かせなくなる**。
- 代わりに **ドロップ先に「ここに入る」の薄いチップ（ゴースト）が出て、掴んでいる元は薄くなる**（v0.124.2）。
  ＝掴んでいることは分かる。
- 🔴 **ゆうた確定＝このままでよい。** 矢印＋箱に寄せるための細工（`copy` などの特殊カーソル）は
  **やらない**。⚠ OS標準の絵が無いカーソルは、環境によって黒フチが落ちて見えなくなる
  （2026-08-16 にパーの手をやめた理由とまったく同じ）。**蒸し返さないこと。**

---

## 🗂 触ったところ

| どこ | 何 |
|---|---|
| `pitflow\js\shaken.js` | `shkSlot()` を新設。`shkFix` と塗り一式を削除。空きマス・帯の `onclick` と吹き出しを付け替え。使わなくなった `_shkSubs` も削除 |
| `pitflow\css\shaken.css` | `.shk-gsc.paintable{cursor:cell}` → `.shk-gsc.slotcell{cursor:pointer}`。`.paintsel` を削除 |
| `pitflow\js\help-content.js` | 車検予定のヘルプを新しい操作に。表の下の3つの箱の説明も追加 |
| `pitflow\index.html` | 版3か所を **v1.118.0**、`shaken.js?v=23`、`shaken.css?v=16`、`help-content.js?v=27` |
| `pitflow\test_shaken_ops.mjs` | **新設**（見張り） |

---

## ✅ 確かめたこと

`pitflow\test_shaken_ops.mjs`（新設）＝ **21 OK / 0 NG**

- 古い作り（押したら決定・範囲塗り・`onmousedown`/`onmouseenter`）が**1つも残っていない**
- 空きマスを押すと枠が1つ入る／入れた枠が帯になる／吹き出しが新しい言い方
- 🔴 **帯を押しても決定にならない**（決定バンドにチップが出ない）。押すと枠が外れる
- 🔴 **決定枠へドラッグした時だけ決定になる**（チップが1枚出る）
- カーソルはマス・帯・チップとも**指さし**／CSSに `grab` も `cell` も無い
- 画面のJSエラー 0

ほか、既存の見張りも緑のまま＝`test_shaken_unsched.mjs` **43 OK / 0 NG**／`test_shaken_share.mjs` **27 OK / 0 NG**。

```
python3 -m http.server 8975    ← 別ウィンドウ
node test_shaken_ops.mjs
```

⚠ 実機（タッチ）での確認はしていない。**タブレットで枠の付け外しと決定のドラッグを1回ずつ試すこと。**

---

## 🚀 出しかた

```powershell
powershell -File "D:\Claude\アプリ開発\deploy-pitflow.ps1" -Message "v1.118.0 車検予定の表＝予定は押すだけ・決定はドラッグだけ／カーソルを指さしに"
```

⚠ `_shared` と `firestore.rules` は触っていない。MHS・CarFlow も触っていない。

### 📣 お知らせ
🔴 **足すか要相談。** 今回は**操作そのものが変わる**（今まで帯を押せば決定できた）。
現場が「決定できなくなった」と感じる可能性があるので、ゆうたの判断で1本出すのが安全。

---

## ⏭ 次にやること

1. 🔴 **未入庫（来なかった車）になると、車検予定から黙って消える。**（v1.115.0 から持ち越し・ゆうたに要確認）
2. 前からの持ち越し
   - `mydash.js` の車検BOXがまだ独自に数え直している
   - 予約詳細の「陸運局休」の判定が車検ボードと別実装
   - LINE画像まわりに自動試験が1本も無い
