# 引き継ぎ 2026-08-20 ／ PitFlow **v1.159.0**
## 区切りラインを **ダブルクリック＝名前と色を直す窓**（7色・線ごと）

> 🗣 ゆうた「**タスクビューのライン機能で、ダブルクリックして名前編集画面に。
> 　　色も複数色用意して、点線から含め色を個別に変えられるように。色は5〜7色ぐらいが普通かな？**」

---

## ✅ ゆうた確定

| | |
|---|---|
| 色の数 | **7色**（オレンジ・赤・緑・青・紫・ピンク・グレー） |
| 線の種類 | 🔴 **点線のまま**（実線・太線は作らない） |
| 変わる範囲 | 🔴 **その線だけ**（ほかのラインは巻き込まない） |

- **オレンジが既定**＝**いま盤面にある線の色は1本も変わらない**（`color` が無いラインはオレンジ）
- 点線も文字の札も**同じ色**になる

---

## 🖥 窓の中身

ダブルクリックで出る（**1回クリックでは出ない**＝ドラッグの掴み損ねで勝手に開かない・v1.37.0 のまま）。

1. **名前の入力欄**（24文字まで。**空にすると線だけ**に戻る＝v1.37.0 の決めごとはそのまま）
2. **色見本7つ**。🔴 **丸い色玉ではなく、実物と同じ点線**で出す
   ＝「その色で線を引いたらどう見えるか」が選ぶ前に分かる
3. 🔴 **「見え方」**＝**盤面に出るのと同じ線＋名前**を窓の中に出す。
   **押した瞬間に変わる**（決定を押す前に確かめられる）
4. **やめる／決定**。⚠ **やめる・外側を押す・Esc は1つも変えない**（決定を押すまで保存しない）

---

## 🧭 作りの決めごと

### 🔴🔴 色の表は `board-line.js` の `COLORS` 1本

```js
{ k:'red', n:'赤', c:'#ef4444', d:'#b91c1c' }
```

- `c` … 線と文字の色 ／ `d` … **明るいテーマ用の濃い方**
  （薄い色＝オレンジ・グレーは白い下地に溶けるので**文字だけ**一段濃くする。線の色みは変えない）
- **CSS には色を書かない。** `polish.css` は `--kbl` / `--kbl-s` / `--kbl-e` / `--kbl-h` / `--kbl-g`
  という**CSS変数を使うだけ**。変数は JS が線に直接付ける
  ＝ **色を足す・色みを直す時は `COLORS` に1行足すだけ**で、線も色見本もゴーストも一緒に変わる
- 🔴 **保存するのは色の名前（`'red'`）だけ。`#ef4444` は保存しない**
  ＝ あとで色みを直したくなった時に、保存済みのラインも一緒に直る

### 🔴 窓は board-line.js の中で完結させた

**共通の窓（`UI.prompt`）は使えない。** あちらは `_shared\ui-dialog.js` が本体で、
色見本を足すと**全アプリ共通の部品**を触ることになる。区切りラインは**PitFlowだけの機能**なので、
窓もこのファイルの中に持つ。⚠ **見た目は ui-dialog に合わせてある**（違う窓が2種類あるように見せない）。

### ゴーストも同じ色にした

動かしている最中に出る太い線（ゴースト）も、**掴んでいる線の色**で出す。
＝ 赤い線を掴んでいるのにオレンジのゴーストが出る、という取り違えを起こさない。

### 保存の形

```
{ id, boardId, status, after, label, color }
```
`color` … `'orange'` / `'red'` / … （`COLORS` のキー）。**無ければオレンジ**。
🔴 全員で共有（`state.settings.boardLines`）なのは今までどおり
＝ **色分けも課の共通認識になる**（「今日はここまで」は赤、「納車便まで」は青、など）。

---

## 🧪 見張り

`test_board_line.mjs` を **53 → 76 OK / 0 NG**。足したのは：

- 色が7色ある／既定はオレンジ／**色が入っていない昔のラインもオレンジで出る**／
  🔴 **知らない色の名前は黙って消さず既定に戻す**
- ダブルクリックで窓が出る・色見本が7つ・今の名前が入っている・**今の色に印**
- 🔴 **「見え方」が窓に出る**／**押した瞬間に見本がその色になる**／**決定の前は保存しない**
- 🔴 **やめる＝色も名前も1つも変わっていない**
- 🔴 決定で保存される／🔴🔴 **盤面の点線と文字がその色になる**／🔴 **点線のまま**
- 🔴🔴 **もう1本のラインは巻き込まれていない（線ごとに個別）**
- 名前を空にすると線だけに戻る（**色は残る**）
- 🔴 **色の表が CSS に書き写されていない**（決めごとを見張る）

### ⚠ 直した所（試験の作りの話）

前の ⑦ は **`UI.prompt` を差し替えて自動で答える**形だった。
窓が自前になった v1.159.0 では、これだと **窓が開きっぱなしになって、そこから先の操作を全部ふさぐ**。
→ **実物の窓を触る**形に直した（`.kbl-in` に打って `.kbl-ok` を押す）。
🔴 **窓を差し替えた時は、その窓を偽物で置き換えている試験も一緒に直すこと。**

### 🔍 見張り98本を全部通した

**壊した所は無し。** 落ちているのは**前から落ちていた7本だけ**（v1.148.0 と同じ数字）。
`test_line_myonly`（担当車両のまとまりバー＝同じ `.kb-line` を使う）も **25 OK / 0 NG**。

---

## 次にやること（持ち越し）

- ⏭ **前から落ちたままの7本**を一度まとめて片付けたい。
- 色を増やしたくなったら **`board-line.js` の `COLORS` に1行**。CSSは触らなくてよい。

---

## デプロイ

```
powershell -File "D:\Claude\アプリ開発\deploy-pitflow.ps1" -Message "v1.159.0 タスクボードの区切りラインをダブルクリック＝名前と色を直す窓（7色・線ごとに変わる・点線のまま）"
```
