# 引き継ぎ 2026-08-18 ── 🚿 受注のときに「洗車」を先に決める（PitFlow **v1.122.0**）

> 🔴 **ゆうたはコードを読まない。** 話すときはファイル名・関数名・行番号を出さない。

---

## 🗣 依頼

> 「タスクボード内、**連絡中→パーツ待ちへの移動時のポップアップ**で
> 　**車販部門への依頼の上に**、車販への依頼と同じ感じで**もうひと枠**作成し
> 　中に **洗車 要 不要 1行メモ** を入れる。
> 　フォロー文として『**既に決まっていれば入力してください。洗車依頼枠に表示されます。後から変更もできます。**』も伝える。
> 　スイッチでの挙動はあくまで**カード詳細のスイッチ操作で**、完TEL依頼でも同様に、
> 　**同じスイッチでいずれからでも同じスイッチをいじる**イメージ」
> 　「これは**お知らせにも**入れてほしい。**早い段階で洗車を確定させて、車販のスケジュールを楽にする**イメージ」

---

## 🔴🔴 決めごと

### 入れ物は増やしていない。**同じ1つのスイッチ**
- 中身は今までどおり `needWash` と `washNote`。
  ＝**カード詳細の洗車のスイッチ／完TELの窓の洗車と、まったく同じもの**。
  どこから触っても同じ1つを動かしているだけなので、**片方だけ違う、は起きない**。
- ⚠ 新しい欄を作らないこと。作った瞬間に「どっちが本当？」が生まれる。

### 🔴 押されていなければ書き換えない
- 既定で「不要」を光らせると、**まだ決まっていない車が全部「洗車しない」で確定**してしまう。
  なので**どちらも光らせずに開き、押された時だけ**カードに書く。
  （2026-08-13 の決めごと「覚えていないものを本物と思い込まない」と同じ考え方）
- **すでに決まっている車**（完TELを通った・「要」になっている・備考が入っている）は、
  その状態を光らせて開く＝**入れ直させない**。
- 備考はいまの値を入れて開いているので、そのまま保存してよい（消したい時も消せる）。

### 置き場所・見た目
- **車販部門への依頼の枠の上**。箱の見た目は車販依頼と同じ（`pp-sales` を共用）。
- 要／不要のボタンは**完TELの窓と同じ部品**（`.rp-chips` / `.rp-chip`）を使い回した。

---

## 🗂 触ったところ

| どこ | 何 |
|---|---|
| `pitflow\js\phase-popup.js` | 受注（order）の窓に洗車の枠を追加。`onWash()` と「押された時だけ書く」`_washPick` |
| `pitflow\css\card-hover.css` | `.pp-wash` / `.pp-washhint`（フォロー文） |
| `pitflow\js\news-pit.js` | 🔴 **お知らせを1件追加**（ゆうた指定。先頭に `n-20260818-washorder`） |
| `pitflow\js\help-content.js` | 車販作業のヘルプに1行 |
| `pitflow\index.html` | 版3か所を **v1.122.0**、`phase-popup.js?v=18`・`card-hover.css?v=23`・`news-pit.js?v=30`・`help-content.js?v=30` |
| `pitflow\test_order_wash.mjs` | **新設**（見張り） |

---

## ✅ 確かめたこと

`pitflow\test_order_wash.mjs`（新設）＝ **28 OK / 0 NG**

- 枠が出る／見出しが「洗車」／要・不要・1行メモがある／**車販依頼の上**にある
- 🔴 **フォロー文が一字一句そのまま**出ている
- 🔴 まだ決まっていない車は**どちらも光らない**／**押さなければ書き換わらない**（移動は今までどおり進む）
- 押すと保存される（要・不要・備考）
- すでに決まっている車・完TELを通った車は、その状態で開く
- 🔴 **カード詳細で「要」にすると受注の窓にもそのまま出る／窓で変えるとカードも変わる**（同じスイッチ）
- お知らせがいちばん上にある／ねらい（車販の段取り）・押さなければ変わらない・同じスイッチ、が書いてある
- 画面のJSエラー 0

⚠ 実際にカードを**ドラッグして**動かす経路（`dnd.js` / `task.js`）は自動では試していない
　（窓を出すところを直接呼んで見ている）。**出したあと1回ドラッグで試すこと。**

---

## 🚀 出しかた

```powershell
powershell -File "D:\Claude\アプリ開発\deploy-pitflow.ps1" -Message "v1.122.0 受注（連絡中→パーツ待ち）の窓で洗車を先に決められるように＋お知らせ1件"
```

⚠ `_shared` と `firestore.rules` は触っていない。MHS・CarFlow も触っていない。

### 📣 お知らせ
🔴 **入れた**（ゆうた指定）。`n-20260818-washorder` ／ v1.122.0。
⚠ お知らせは**1回のログインで3件まで**。貯めないこと。

---

## ⏭ 次にやること

1. ホバー詳細（`card-hover.js`）の車両注意の長い言い方も本家から出すか。
2. 済にする**前**の予約詳細にも陸運局とRを出すか／MHS・前日LINEにも出すか。
3. 🔴 **未入庫（来なかった車）になると、車検予定から黙って消える**（v1.115.0 から持ち越し・要確認）
