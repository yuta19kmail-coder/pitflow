# 引き継ぎ 2026-08-18 ── 当日ビューの「1課／2課」バッジを薄くしない（PitFlow **v1.114.0** ／ MHS **v1.30.0**）

> 🔴 **ゆうたはコードを読まない。** 話すときはファイル名・関数名・行番号を出さない。
> 　　この書き置きは「次の Claude」向けなので、ここには書いてよい。

---

## 🗣 依頼

> 「次、**PitFlowとMHS同じで** フロントが入ってない時の**1,2課のバッジが今ちょっと薄い設定**だが、
> 　**他のピンクとグリーンと同じでいい**。**コバモのグレーはそのまま**」

---

## 🔎 何が「薄い」だったか

v1.98.0（PitFlow）／v1.10.0（MHS）で課のバッジを作った時に、
**「人ではない」と分かるように少しだけ落とす**という作りにしていた＝

- `opacity: .82`（＝18%うすい）
- 内側に白いフチ（`box-shadow: inset 0 0 0 1px rgba(255,255,255,.28)`）

これを**両方やめた**。字の間隔だけ残す（`letter-spacing:0`）。
＝**人の名前のバッジとまったく同じ濃さの緑・ピンク**になる。

⚠ 「人ではない」ことは **字（1課・2課）** で分かる。色を落とす必要はない、というのが今回の指定。

### 🔴 コバモ（自社）のグレーは触っていない

グレーは **課が選ばれていない時の色**（`pit-share.js` の `PIT_DIV_NONE_COLOR = '#8390a6'`）で、
出ているのは**人のバッジ側**（フロント担当＝小林モータース＝「コバモ」）。今回の場所とは別。
🔴 **色はいまも pit-share.js の1本だけ。CSS に写していない**（見張りを足した。下記）。

---

## 🗂 触ったところ

| どこ | 何 |
|---|---|
| PitFlow `pitflow\css\views.css` | `.tr-front.is-div` から `opacity` と `box-shadow` を削除。`views.css?v=47 → 48` |
| PitFlow `pitflow\index.html` | 版 3か所（meta / ログイン / トップバー）を **v1.114.0** へ |
| MHS `index.html` | 同じ `.tr-front.is-div` を同じ形に。版 3か所を **v1.30.0** へ |

⚠ **当日ボードの CSS は MHS（1枚もの）と PitFlow（views.css）で別々に書いてある。**
　 片方だけ直す事故が起きやすい。だから下の見張りで**両方**を見るようにした。

---

## ✅ 確かめたこと

`MHS\test_pit_sync.mjs` = **73 OK / 0 NG**（**+4本**）
- 🔴 課のバッジは人の名前と**同じ濃さ**（`opacity` が 1 で、人と同じ）
- 🔴 内側の白いフチも入っていない（人と同じ）
- 🔴 **PitFlow 側の `views.css` も**薄くしていない・フチを入れていない（ファイルを直接読む）
- 🔴 **コバモのグレーは pit-share.js の1本のまま**（CSS にも MHS にも写していない）

ほか MHS 5本も緑（`test_portrait_url` 93/0 ／ `test_pit_today` 113/0 ／ `test_mhs_share` 48/0 ／
`test_lineshare` 36/0 ／ `test_icons_dialog` 122/122）。

`PitFlow\pitflow\test_today_div.mjs` に**同じ2本を足した**（人と同じ濃さ／フチなし）。
⚠ **この1本はクラウド側では走らせていない**（PitFlow の全部（js 92本・css 23本）を持ってこないと動かないため）。
　 代わりに上の MHS 側の見張りが **PitFlow の views.css を直接読んで**同じことを見ている。
　 手元で走らせる時＝`python3 -m http.server 8997` → `node test_today_div.mjs`。

### 走らせ方（MHS のフォルダで）
```
python3 -m http.server 8801 --directory .
PORT=8801 PITFLOW_DIR=..\PitFlow\pitflow\js node test_pit_sync.mjs
```

---

## 🚀 出しかた

```powershell
powershell -File "D:\Claude\アプリ開発\deploy-pitflow.ps1" -Message "v1.114.0 当日ビューの課のバッジを薄くしない（人と同じ濃さの緑・ピンク）"
```

```powershell
powershell -File "D:\Claude\アプリ開発\deploy-one.ps1" MHS -Message "v1.30.0 当日ビューの課のバッジを薄くしない（PitFlow v1.114.0 と同じ）"
```

⚠ `_shared` と `firestore.rules` は触っていない。

### 📣 お知らせ
**足していない。** 見た目が少し濃くなるだけで、みんなの操作は変わらないため。

---

## ⏭ 次にやること

1. 当日ボードの CSS が MHS と PitFlow で**二重に書いてある**問題。
   いまは見張りでそろえているが、いずれ1本にまとめたい（`_shared` 行き）。
2. 前からの持ち越し
   - 前日LINEの画像に当日メモ（代車名）を出すか／右の「代車」バッジを残すか
   - 縦ビューで部署の予定が長い日の畳み方
   - LINE画像まわり（`mhs-board.js` / `board-image.js`）に自動試験が1本も無い
   - `mydash.js` の車検BOX／予約詳細の「陸運局休」／`modal-outside.js` を `_shared` へ
