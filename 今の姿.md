# PitFlow ─ 今の姿（2026-09-12 時点）

> 🔴 **ここは「いまどうなっているか」だけ。履歴は書かない。**
> 　 履歴は `..\..\_記録\作業ログ.md` を **PitFlow** で引く（新しい順）。
> 🔴 **上書きして最新に保つ。積まない。** 50行以内。
> 　 決まり＝`..\..\開発全体メモ.md` の「このメモの更新ルール」。

## これは何か

**整備工場の業務。** 入庫 → 預かり中の進捗 → 実績化 までを1つで完結。当日ビュー・代車・車検・売上。

## 版と置き場

| | |
|---|---|
| **版** | **v2.109.0** 🔴 正は `index.html` の `app-version`。ここが古くなったら直す |
| **画面** | https://pitflow.kobayashi-motors.com |
| フォルダ | `CoreFlowアプリ\PitFlow\pitflow\` |
| GitHub | `yuta19kmail-coder/pitflow` |
| **出し方** | `powershell -File "D:\Claude\CoreFlowアプリ\deploy-pitflow.ps1"` |

## 作り

`index.html` 1,017行＋**js 127本**（このシリーズで一番大きい）。
大きいもの＝`card-view.js` 213KB／`card-detail.js` 195KB／`customers.js` 141KB。
配信先＝`pitflow`。`firebase.json` に **functions もある**。

## 🔴 触る時の勘どころ

- 🔴 **本番か練習かを自分で見分けている。** `js/firebase-init.js` の `CLOUD_HOSTS` にあるURLで
  　開いた時だけ本番。それ以外は**端末保存の練習モード**になる
- 🔴 **版番号でサーバーが弾く仕組みが効いている。** 古い画面から保存しようとすると拒否される
  　（→ `..\..\開発全体メモ.md` の「版番号でサーバーが弾く」）
- **体験版（PitFlow-Demo）は直接触らない。** `make-demo.ps1` で作り直す写し
- 見張り（自動の試験）が `_見張り\` にある。直したら走らせる
  　・node だけの見張り＝`node _見張り	est_〜.mjs`（PitFlow のフォルダで）
  　・ブラウザを使う見張り（playwright）＝先に `python -m http.server <見張りに書いてある番号>` を別窓で立ててから同じように。
  　　この PC で走る仕掛け＝`node_modules` は **CarFlow の node_modules へのジャンクション**／ブラウザの場所は `_見張り\_chrome.mjs` 1本
  　⚠ **前から赤いもの**（2026-09-13 夜 時点）＝`test_mech_none` 2NG ／ `test_inspect` は L08（きれいな2枚から所見）と画面の読み取りで途中で止まる／`test_quarter_saved` 43/1（版の印）／`test_cust_loanercar` 23/1／`test_deeplink`・`test_loaner_cal`・`test_loaner_free`・`test_loaner_name`・`test_mhs_link` は途中で止まる
- 🔴 クォーターチェックの印は **`pitSettings/qmarks-map`（1件ずつ merge）**。前の `qmarks`（一覧）は読むだけ（v2.105.0）
- 🔴 リース車両＝車の `lease`／`leaseUp`（暫定・'YYYY-MM'）／`leaseUpFixed`（確定・日）。使う日は `pitLeaseEnd` 1本（v2.107.0）

## ⏭ まだ確かめていないこと

- 127本の中身は読み切っていない。**触る所だけ読む**のが前提
- ⏭ **次にやる＝「開くたびに全件を2回読む」を軽くする**（→ `..\..\開発全体メモ.md` の「次にやる」）
- 📡 CoreFlow のダッシュボードへ概況を配る＝`js/app-summary.js`（本番ログイン中・10分ごと）。数え方は mydash.js の `PIT_DASH_API` を借りるだけ（v2.108.0）
- 📌 付箋ボードは全アプリ共通の部品（`js/coreflow-note-board.js`・本体は `_shared`）。`js/board-notes.js` は保存先と名簿を渡すだけ。済は3日で隠す（消さない）（v2.109.0）
