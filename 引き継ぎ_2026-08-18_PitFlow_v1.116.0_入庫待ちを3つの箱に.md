# 引き継ぎ 2026-08-18 ── 🔧 入庫待ちの予約を「今週／来週／再来週」の3つに（PitFlow **v1.116.0**）

> 🔴 **ゆうたはコードを読まない。** 話すときはファイル名・関数名・行番号を出さない。
> 　　この書き置きは「次の Claude」向けなので、ここには書いてよい。
> ⚠ **v1.115.0（同じ日）の続き。** 先にそちらを読むこと
> 　　＝`引き継ぎ_2026-08-18_PitFlow_v1.115.0_車検予定に終わった車が居座る.md`

---

## 🗣 依頼（v1.115.0 を出したあと）

> 「OK 取り合えず　**かなり減った**
> 　で**今何順に並んでるのかわからない**けど
> 　まずグループを **今週入庫分（金曜日まで）**と**来週入庫分（土曜から翌週金曜日まで）**
> 　**再来週分（翌週土曜日〜その次の金曜日まで）**と3つに分ける
> 　でそれぞれの**入庫日順**に並べる
> 　**全体の場所を入れ替えて上からメインの表、今週、来週、と下に続くように**する
> 　**8/22入の表記を8/22(土)と曜日にする**
> 　**左とMTなどのバッチがある車は今週、来週予定にも小さく表示**したい」

---

## 🗂 やったこと（6つとも指定どおり）

| | 何 |
|---|---|
| ① 3つに分ける | **今週入庫分**（〜今週の金曜）／**来週入庫分**（土〜翌金）／**再来週入庫分**（その次の土〜金） |
| ② 並び | それぞれ**入庫日順**。同じ日は名前順（毎回同じ並びになるように） |
| ③ 置き場所 | 帯を**表の上から下へ移した**。上からメインの表 → 今週 → 来週 → 再来週 |
| ④ 日付 | `8/22入` → **`8/22(土)入`**。見出しの期間にも曜日を入れた |
| ⑤ バッジ | **左・MT・12点**をチップにも小さく出す（ガントの行と**同じ言葉**＝`carAttrs` 1本） |
| ⑥ 見出し | 各箱に**期間と台数**（例：`来週入庫分 8/22(土) 〜 8/28(金) 17台`） |

### 🔴 決めごと（次に触る人が迷わないように）
- **週の区切りは土曜はじまり・金曜おわり**（ゆうた指定）。土日が休みなので金曜で締める。
- **数える起点は「今日」。** カレンダーを前週・次週に送っても3つの箱は動かない。
  ⚠「今週」「来週」は**今日から見た言葉**なので、めくると意味が変わってしまうため。
- **入庫日が過ぎているもの**（＝仮予約・承認待ち。本予約は自動で未入庫に落ちる）と
  **入庫日未定**は、いちばん手前の「**今週入庫分**」に入れる＝目に入る場所へ。入庫日未定は箱の最後。
- **再来週より先の予約は出さない**（今までどおり）。
- **空の週も見出しだけは必ず出す。** 黙って消えると「壊れて出ていない」のか
  「本当に無い」のか分からない（8/18 の「動いてない？」がまさにそれ）。

---

## 🗂 触ったところ

| どこ | 何 |
|---|---|
| `pitflow\js\shaken.js` | `fmtMDW()`（曜日つき）・`addDays()`・`carAttrs()`・`waitGroups()`・`buildWait()` を新設。帯の組み立てを表の後ろへ移動。ガントの行も `carAttrs` を使うように |
| `pitflow\css\shaken.css` | `.shk-wait` / `.shk-wg` / `.shk-wgh` / `.shk-wgr` / `.shk-wgn` / `.shk-wgb` / `.shk-unm` / `.shk-ucar` を新設。`.shk-uchip` を横並び（flex）に。`.shk-ures` を先頭配置＋太字 |
| `pitflow\index.html` | 版3か所を **v1.116.0**、`shaken.js?v=20 → 21`、`shaken.css?v=14 → 15` |
| `pitflow\test_shaken_unsched.mjs` | 見張りを **+20本**（合計 42本） |

🔴 **本家の物差し `pit-share.js` は今回も1文字も触っていない。** MHS・前日LINEの画像に影響なし。

---

## ✅ 確かめたこと

`pitflow\test_shaken_unsched.mjs` ＝ **42 OK / 0 NG**（v1.115.0 の 22本＋今回 20本）

- 3つの箱ができる／**金曜ちょうど・土曜ちょうど**が正しい側に入る／再来週より先は出ない
- 過ぎたぶん（仮予約）と入庫日未定は今週の箱・未定は最後
- **入庫日順に並ぶ**（今週・来週とも）
- **メインの表が先、3つの箱が最後**（並び順をDOMで確認）
- 日付が `◯/◯(曜)入` の形／来週の初日は必ず `(土)`／見出しの期間にも曜日
- 左・MT・12点のバッジが出る／何も無い車には出さない
- 画面のJSエラー 0

`test_shaken_share.mjs` は **27 OK / 0 NG** のまま。

```
python3 -m http.server 8973    ← 別ウィンドウ
node test_shaken_unsched.mjs
```

⚠ 見た目そのものの自動試験は無い。**出したあと実物で見ること**（本文で確認したのはデモの実データ）。

---

## 🚀 出しかた

```powershell
powershell -File "D:\Claude\アプリ開発\deploy-pitflow.ps1" -Message "v1.116.0 入庫待ちの予約を今週・来週・再来週の3つに（入庫日順・曜日つき・左MTバッジ・表の下へ）"
```

⚠ `_shared` と `firestore.rules` は触っていない。MHS・CarFlow も触っていない。

### 📣 お知らせ
**足していない。** 車検予定の見え方が変わるだけで、みんなの操作は変わらないため。

---

## ⏭ 次にやること

1. 🔴 **未入庫（来なかった車）になると、車検予定から黙って消える。**（v1.115.0 から持ち越し）
   陸運局へ行くと決めた日があっても印ひとつ残らない。直すなら**本家（`pit-share.js`）**＝
   MHS の当日ビューと前日LINEの画像にも効く。**ゆうたに聞いてから。**
2. 3つの箱は**入庫日順**だが、決定バンドの並びは別（午前→午後→未実施→名前）。混同しないこと。
3. 前からの持ち越し
   - `mydash.js` の車検BOXがまだ独自に数え直している（3つ目の写し）
   - 予約詳細の「陸運局休」の判定が車検ボードと別実装
   - LINE画像まわり（`mhs-board.js` / `board-image.js`）に自動試験が1本も無い
