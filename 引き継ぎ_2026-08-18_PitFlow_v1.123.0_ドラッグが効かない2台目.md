# 引き継ぎ 2026-08-18 ── 🖱 車検予定「2台目が動かせない」を直す（PitFlow **v1.123.0**）

> 🔴 **ゆうたはコードを読まない。** 話すときはファイル名・関数名・行番号を出さない。
> ⚠ **v1.119.0 で自分が作った不具合**。同じ日の書き置きを先に読むこと。

---

## 🗣 依頼

> 「さっきの車検スケジュールの**ドラッグが効かないところがある。2台目がうごかせないのかな？** 確認して」

---

## 🔎 開けてみたら ── **決定チップが縦に伸びて、下の行が画面の外へ押し出されていた**

v1.119.0 で決定チップの下に「**回送 未定／陸運局 未定／R未定**」の3枚を並べた。
枠は1つ **118px しかない**ので3枚が**折り返して3段**になり、**チップ1枚が約95px**（前は約40px）。
決定枠に何台か入るとバンドが一気に伸び、**下のガントの行が画面の外へ出て掴めなくなる**。

🔴 **実際に再現した。** 画面 1366×768・6台のとき、**6台目の帯が y=807**＝画面の下（768）より外。
　 掴もうとしても届かない＝「動かない」。**ドラッグの仕組み自体は壊れていない。**

---

## 🗂 直したこと

### ① チップの拡張を**必ず1行**にした（これが本丸）
- 並べる順を **R → 担当 → 陸運局** に。前の2つは短いので必ず出る。
  **長い陸運局の名前だけが縮んで「…」**になる（全文は吹き出しで出す）。
- **未定は1枚にまとめる**（例：`未定 R・回送・陸運局`）。3枚並べない。
- 折り返さない（`flex-wrap:nowrap`）。
- 🔴 結果＝**チップ約95px → 約53px**。1366×768 で6台目の帯が **y=807 → y=621**（画面の中）。

### ② ドラッグ中に端まで来たら**自分でスクロール**するようにした
- 掴んでいる間は指を離せないので、**画面の外にある行や日には届かなかった**（台数が増えるほど起きる）。
- **縦**＝車検予定の画面そのもの（`.view` が overflow:auto。窓は動かない作り）。
  **横**＝表の横スクロール（`.shk-scroll`）＝**先の日付へも運べる**ようになった。
- 端の帯は **48px**。⚠ 広くすると「見えた枠の上に指を置いているのにまだスクロールし続ける」で
  狙った枠が通り過ぎる。**広げないこと。**
- 端で止まっている間も**ドロップ先を見直す**（動かさないと枠が光らない、を防ぐ）。
- **指を離したら必ず止める**（止め忘れると裏で走り続ける）。

---

## 🗂 触ったところ

| どこ | 何 |
|---|---|
| `pitflow\js\shaken.js` | `chipMeta` を1行に（順番・未定を1枚に）。`_autoScrollStart/Stop`・`_updateZone` を新設し、`pointermove` から呼ぶ形に整理 |
| `pitflow\css\shaken.css` | `.shk-meta` を `nowrap`＋`overflow:hidden`、`.shk-mt.of` / `.tbd` だけ縮む |
| `pitflow\index.html` | 版3か所を **v1.123.0**、`shaken.js?v=26`・`shaken.css?v=19` |
| `pitflow\test_shaken_ops.mjs` | 見張り **+7本**（29 OK） |
| `pitflow\test_shaken_decide.mjs` | 未定の出し方が変わったので見張りを直した（33 OK） |

---

## ✅ 確かめたこと

- `test_shaken_ops.mjs` ＝ **29 OK / 0 NG**（**+7本**・画面は 1366×768 で見ている）
  - 🔴 全部未定でも**1段**／未定は1枚／中身が入っていても1段／チップ 60px 以下
  - 🔴 **5台決定していても、残りの行が画面の中にある**（今回の不具合そのもの）
  - 🔴 上の端まで運ぶと**自分でスクロールする**／**指を離したら止まる**
- 実際にマウスでドラッグする試験も回した＝**6台とも動く**（直す前は6台目が動かなかった）。
  14台まで増やしても、端まで運んで枠が見えたら落とせることを確認。
- ほかも緑＝`test_shaken_unsched` 48 ／ `test_shaken_decide` 33 ／ `test_shaken_detail` 25 ／
  `test_order_wash` 28 ／ `test_shaken_share` 27。

⚠ **タッチ（タブレット）では試していない。** 自動スクロールは指でも効くはずだが、実機で1回試すこと。

---

## 🚀 出しかた

```powershell
powershell -File "D:\Claude\アプリ開発\deploy-pitflow.ps1" -Message "v1.123.0 車検予定＝決定チップを1行に戻して下の行が押し出されないように／ドラッグ中の自動スクロール"
```

⚠ `_shared` と `firestore.rules` は触っていない。MHS・CarFlow も触っていない。

### 📣 お知らせ
**足していない**（v1.119.0 で出た不具合の手当てなので）。

---

## ⏭ 次にやること

1. 🔴 **画面に足すものは「1行に収まるか」を必ず見る。** 今回は3行に増えただけで
   **下の行が画面外へ出て操作できなくなった**。見た目の話に見えて、操作を殺す。
2. 済にする前の予約詳細にも陸運局とRを出すか／MHS・前日LINEにも出すか。
3. 🔴 未入庫（来なかった車）になると車検予定から黙って消える（v1.115.0 から持ち越し・要確認）
