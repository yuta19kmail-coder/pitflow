# 引き継ぎ 2026-08-20 ／ PitFlow **v1.157.0**
## 代車の「まで」が**入っていない間＝検討中**は、
## **最短入庫日も、カレンダーの緑の帯も、打つそばから動く**

> 🗣 ゆうた「**代車の「まで」が入ってない間（いわば検討中の段階）であれば、
> 　　作業タイプや国産／輸入車チップ、手入力の概算預かり日数で
> 　　最短入庫日、そしてそれに伴うカレンダーの透過グリーンの表示がリニアに変わるようにしてほしい**」

（v1.156.0 の続き。先に `引き継ぎ_2026-08-20_PitFlow_v1.156.0_最短入庫日の作り直し.md` を読むこと）

---

## 🔎 なにが起きていたか

v1.156.0 で **最短入庫日**と**緑の帯**を作ったが、**描き直しの合図が足りていなかった。**

| 触った所 | v1.156.0 | |
|---|---|---|
| 作業タイプ・国産／輸入・受付タイプの**チップ** | ✅ 動いていた | 押すと `renderCardForm` で全部描き直すため |
| 🔴 **概算 預かり日数を手で打った時** | ❌ **動かなかった** | 数字を打っても最短入庫日も帯もそのまま |

＝ **お客様と電話しながら「じゃあ5日みますね」と打っても、案内する日付が変わらない。**
古い日付のまま口に出してしまう筋。🔴 **エラーは1つも出ない**タイプ。

---

## ✅ どう直したか ─ **打っている最中は「その2か所だけ」描き直す**

新設 **`card-detail.js` の `pitCfPlanSync(カード)`**。

### 🔴🔴 なぜ `renderCardForm`（全部描き直し）を呼ばないか

**呼んではいけない。** 打っている最中に全部を作り直すと、

1. **入力欄から焦点が飛ぶ** ＝ 2文字目が打てない（「15」と打てず「1」で止まる）
2. **代車ガントのドラッグが効かなくなる** ＝ つかむ相手（`#cfs-lg-body` の中身）が
   丸ごと入れ替わり、直に付けた手が離れるため

👉 だから **描き直すのは2か所だけ**：

| | どう直すか |
|---|---|
| **最短入庫の枠** | `.cfs-short[data-team]` を `outerHTML` で差し替え（中のボタンは `onclick` 属性なので付け直し不要） |
| **緑の帯** | `tr` の **クラスを付け外しするだけ**（中身は1つも作り直さない＝ドラッグの当たり判定が生きたまま） |
| **帯の説明** | 文だけ書き換え |

⚠ アイコン（`<i data-ic=…>`）は `pit-icons.js` の見張り（MutationObserver）が
**新しく置かれた所に自動で流し込む**ので、こちらで呼ばなくてよい。

### 呼ぶ所

`estHoldDays` / `loanerFrom` / `loanerTo` の **`input`**（＝1文字ごと）。
チップ類は今までどおり `renderCardForm` で全部描き直す（押した瞬間なので焦点が飛んでも困らない）。

---

## 🔴🔴 「まで」が入ったら、そこで**止める**

**「まで」＝人が決めた合図。** 決めたあとに数字を触っても**帯は動かさない。**

| 代車の「から・まで」 | 帯が指すもの | 上の文 |
|---|---|---|
| **まだ「まで」が空** ＝ 検討中 | いま案内している窓（1週間 or 預かり＋前後1日） | 「この入庫日で押さえる幅」／「いま案内している最短の幅」 |
| 🔴 **「まで」が入った** ＝ 決まった | **その貸出の期間そのまま**（前後の予備を足さない） | 🔴 **「決まった貸出の幅」** |

- 見た目でも見分けが付くよう、決まった時は帯の説明を少し濃くする（`.cfs-lg-bandnote.fixed`）
  🔴 ただし **色だけに頼らない。文言も変える**（v1.156.0 と同じ決めごと）
- **「まで」を消せば、また検討中に戻って動き出す**

---

## 🧭 触った所と、触っていない所

| | |
|---|---|
| 変えた | 打っている最中の**描き直しの合図**（`card-detail.js` の入力ハンドラ） |
| 変えた | 帯の指す先（**「まで」が入ったら決まった貸出の幅**） |
| 変えていない | 🔴 **窓の決め方**（`loaner-free.js` の `pitLoanerPlanWindow` 1本のまま） |
| 変えていない | 実際に貸せるかの判定・代車カレンダーの列 |

🔴 **日数の計算をここに書き写さないこと。** 部品に聞くだけ。

---

## 🧪 見張り

`test_short_intake.mjs` を **33 → 47 OK / 0 NG** に増やした。足したのは：

- 作業タイプ未選択のうちは**1週間ぶんの帯**／説明も「1週間」と言っている
- 🔴 **1文字打った瞬間に帯が「預かり4日＋前後1日＝6日」になる**（左端は入庫日の前日）
- 🔴 最短入庫カードの説明も**その場で**「預かり4日…」に変わる
- 🔴🔴 **打っている欄から焦点が飛んでいない**（＝数字を打ち続けられる）
- 打ち直し（9日）にも追従する・**焦点はまだ入力欄**
- 🔴🔴 **「まで」を入れたら帯は決まった貸出の幅で止まる**（3日ぴったり・予備を足さない）
- 言葉も「決まった貸出の幅」に変わる／見た目も見分けが付く
- 🔴 **「まで」を消せば検討中に戻る**

既存も通した：`test_resv_detail` 128 ／`test_loaner_free` 79 ／`test_loaner_cal` 31 ／
`test_hold_days` 39 ／`test_intake_guard` 37 ／`test_front_days` 29 ／
`test_resv_cancel_loaner` 41 ／`test_return_pending` 47 ／`test_wash_plan` 26 ／
`test_return_gate` 45 ／`test_mydash_view` 24 ／`test_errcode` 21。

---

## 次にやること（持ち越し・v1.156.0 から）

- **ダッシュボードの「代車の最短空き」は 1日空きのまま**（`dashLoanerEarliestFree`）。
  同じ言葉で違う意味になっていないか、一度ゆうたと見た方がいい。
- 商用車（`commercial`）の代車は**国産あつかい**。分けるなら決めごとが要る。
- `CoreFlow_エラー番号一覧.html` の作り直し（`PF-4011`／`PF-3034` の2件ぶん）。
  🔴 手で書かず `_tools\エラー番号_見張りと一覧づくり.py` で。
- アーカイブから戻した時に代車をどうするか（いまは戻らない＝押さえ直し）。

---

## デプロイ

```
powershell -File "D:\Claude\アプリ開発\deploy-pitflow.ps1" -Message "v1.157.0 代車の「まで」が空のうち（検討中）は、概算預かり日数を打つそばから最短入庫日と代車カレンダーの緑の帯が動く／「まで」が入ったら決まった貸出の幅で止まる"
```
