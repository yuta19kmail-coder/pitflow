# 引き継ぎ 2026-08-18 ── 📍 狭い枠の陸運局は「地名だけ」（PitFlow **v1.129.0**）

> 🔴 **ゆうたはコードを読まない。** 話すときはファイル名・関数名・行番号を出さない。
> ⚠ **v1.127.0（担当者の名前）と同じ考え方。**セットで読む。

---

## 🗣 依頼

> 「ついでに　**さっきのフルネームと同じで**　省略表示は　**野田** とか **習志野** とか**地名だけ**でいいよ」

---

## 🔴🔴 決めごと ── 陸運局も **2つの出し方**（担当者とまったく同じ形）

| 使う場所 | 何を出すか | 窓口 |
|---|---|---|
| **カード詳細（予約詳細）・ホバー情報カード** | **正式名のまま** | `pitShakenOffice(c)` |
| **それ以外**（車検予定のチップ・MHS の当日ビュー・前日LINEの画像） | **地名だけ** | `pitShakenOfficeShort(c)` |

**地名の出し方**＝ 後ろの**お決まりの言葉を落とすだけ**（`pitLocShort`）。
- 野田自動車検査登録事務所 → **野田**
- 習志野自動車検査登録事務所 → **習志野**
- 千葉運輸支局 → **千葉**
- 野田自動車検査登録事務所（千葉）→ **野田**（うしろの（かっこ書き）も落とす）

⚠ 落とすものが1つも無ければ**そのまま**出す（知らない書き方でも空にしない）。
⚠ **CoreMembers の名前は書き換えない。** 出す時に短くしているだけ。
⚠ チップの**吹き出し（マウスを乗せた時）には正式名**を出す＝地名だけだとどこか分からない時があるため。

---

## 🗂 触ったところ

| どこ | 何 |
|---|---|
| `pitflow\js\pit-share.js` | **`pitLocShort` / `pitShakenOfficeShort` を新設**。`pitShakenOnDate` の `office` を**地名だけ**に |
| `pitflow\js\shaken.js` | 車検予定のチップを**地名だけ**に（吹き出しは正式名） |
| `pitflow\index.html` | 版3か所を **v1.129.0**、`pit-share.js?v=11`・`shaken.js?v=30` |
| `pitflow\test_shaken_decide.mjs` / `test_shaken_detail.mjs` | 見張り（36 OK ／ 41 OK） |

⚠ カード詳細（`card-view.js`）とホバー（`card-hover.js`）は**触っていない**＝今までどおり正式名。

---

## ✅ 確かめたこと

- `test_shaken_detail.mjs` ＝ **41 OK / 0 NG**
  野田／習志野／千葉の3つとも地名だけになる／知らない書き方はそのまま／
  🔴 **カード詳細の陸運局は正式名のまま**
- `test_shaken_decide.mjs` ＝ **36 OK / 0 NG**
  🔴 **チップの陸運局は地名だけ（正式名を出さない）**／物差しが配るのも地名だけ
- ほかも緑＝`test_shaken_ops` 54 ／ `test_shaken_unsched` 48 ／ `test_order_wash` 28 ／ `test_shaken_share` 27

---

## 🚀 出しかた

```powershell
powershell -File "D:\Claude\アプリ開発\deploy-pitflow.ps1" -Message "v1.129.0 狭い枠の陸運局は地名だけ（野田・習志野）／カード詳細とホバーは正式名"
```

⚠ `_shared` と `firestore.rules` は触っていない。MHS・CarFlow も触っていない。
⚠ ただし **`pit-share.js` は MHS も借りている**。当日ビューと前日LINEの画像に陸運局を出すようになった時は、
　 自動で**地名だけ**になる（いまは出していない）。

### 📣 お知らせ
**足していない**（名前が短くなるだけ）。

---

## ⏭ 次にやること

1. フロント担当・メカ担当など**ほかの担当名**も v1.127.0 の2つに揃えるか。
2. 済にする前の予約詳細にも陸運局とRを出すか／MHS・前日LINEにも出すか。
3. 🔴 未入庫（来なかった車）になると車検予定から黙って消える（v1.115.0 から持ち越し・要確認）
