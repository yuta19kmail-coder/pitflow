# 引き継ぎ 2026-08-22 ／ PitFlow **v1.169.2 → v1.170.0**
## 「点検」→ **データチェック**／**日常チェック・クォーターチェック**の切り替え／🔴 **アーカイブ車両でも「該当箇所だけ」誰でも直せる**

> 🗣 ゆうた（2026-08-22）
> 「ちょっと時間かかったな・・・／でやっとスタートかと思うんだけど
> 　まず **点検→データチェック** に名称変更
> 　でビューの中に **日常チェック** と **クォーターチェック** に一番上部で切り替えられるように
> 　日常チェックは今の物／クォーターチェックはPDFの付け合わせとAIチェックをかけるイメージ
> 　で大きな変化で、ここからは **アーカイブ車両であっても、該当箇所だけは修正をだれでもかけられる**ようにしたい
> 　**ほかの箇所は触れない**
> 　**確定金額と確定日だけはこれまで通り管理者のみ**」

---

## ✅ ゆうた確定（この4つがそのまま today の仕様）

| | 決まったこと |
|---|---|
| 名前 | **点検 → データチェック**。⚠ **車の12ヶ月点検・タスクボードの点検待ちは言い換えない** |
| 中身 | いちばん上で **日常チェック／クォーターチェック** を切り替える |
| 直し方 | 🔴 **アーカイブ済みの車でも、指摘された欄だけは誰でも直せる。ほかの欄は触れない** |
| 守り | 🔴🔴 **確定金額・確定日は、これまで通り管理者だけ** |

---

## ① 名称変更（点検 → データチェック）

**なぜ変えたか**＝PitFlow の「点検」は **車の12ヶ月点検** と **タスクボードの点検待ち** を指す言葉。
同じ字でデータの見直しも呼ぶと、現場で必ず取り違える。

変えた所：左メニュー／ビューの見出し／ボタン（**もう一度点検 → もう一度チェック**）／
上の帯（「◯時◯分◯秒に**チェック**」）／ヘルプ／トースト／書き出しのファイル名
（`PitFlow点検_…json` → **`PitFlowデータチェック_…json`**）。

🔴 **車のほうの「点検」はそのまま。** 見張りで「言い換えすぎていないこと」も見ている
（`pitCardStatusText({status:'check'})` に「点検」が残っているか）。

---

## ② 日常チェック／クォーターチェック（いちばん上で切り替え）

言葉の表は **`inspect.js` の `MODES` 1本**。画面で綴らない。

| | 中身 |
|---|---|
| **日常チェック** | いまの61規則。PitFlow の中だけで分かる食い違い（お金はかからない） |
| **クォーターチェック** | ②売上チェックリストPDFの突合 ＋ ③AIチェック（およそ週1）。**いまは器** |

### クォーターチェックの器に何を出しているか

空の画面で「まだです」と黙るより、**次に何をすれば動くか**を出しておく方が使える。

- いまのクォーター（例：`8月 第3クォーター　2026-08-16 〜 2026-08-23`）
- **② 突合** … 何を突き合わせるか／動かすのに要るもの（PDFと、下の書き出し）
- **③ AIチェック** … 🔴 **いまはまだ足りないもの**を名指しで3つ
  1. **母数**＝担当ごとの担当台数（何台のうち何件かが分からないと人を比べられない）
  2. **車の中身**＝フローとメモ（文章が無いと「なぜ止まったか」が読めない）
  3. **前回の書き出し**（一度きりの出来事と、くり返しているクセを見分けるため）
- いまの日常チェックの件数＋書き出しボタン

> ⚠ この3つは 8/21 の実データ検証で分かったこと（8月頭の一斉入力を「クセ」と読み違えた件）。
> **そろうまで③は動かさない。** 画面にもそう書いてある。

### 🔴 クォーターの区切りは売上の物差しを**借りる**

`sales.js` に **`window.pitQuarterOf(dateStr)`** を新設し、既存の `qOfDay` / `qWindow` を**そのまま**呼ぶ口にした。
＝ `dd<=7?0:…` を画面に書き写さない。区切りを変える日に片方だけ古くなるのを防ぐ。

```js
pitQuarterOf('2026-08-09') // → { y, m1:8, qi:1, no:2, s:'2026-08-08', e:'2026-08-15', label:'8月 第2クォーター' }
```

---

## ③🔴🔴 いちばん大きな変わり目 ─ 「ここを直す」

### なにが困っていたか

本番 377台のデータチェックで出た所見 **260件のうち 129件が返車の済んだ車**だった。
アーカイブまで行った車は「管理者以外は触れない」ので、
**抜けているのが分かっているのに誰も直せない**。＝毎回出続けて、**いま動かせる車が埋もれる**。

### 考え方（ここが設計の肝）

🔴🔴 **開くのはカードではなく「指摘された欄」だけ。**
カード詳細をアーカイブ済みでも開けるようにする、のではない。
データチェックが「ここが空です」と言った**その欄だけ**を小窓に出す。
＝ **ほかの欄は画面に出てこない**ので、触りようがない（ゆうた指定そのまま）。

### 新ファイル `js/inspect-fix.js`

| 表 | 中身 |
|---|---|
| `FIELDS`（`PIT_FIX_FIELDS`） | 直せる欄 **29本**。`{id, label, type, admin?, hint?, get?, set?}` |
| `RULE_FIX`（`PIT_RULE_FIX`） | **規則 → どの欄を直せば消えるか**（38規則にひもづけ） |

- `type` … `text / textarea / date / time / money / tel / pick`
- `pick` の選択肢は **設定の表から引く**（`state.dropTypes` / `repeatTypes` / `workTypes` / `boards` / `staff`）。
  ⚠ 画面で「待・当・預」と綴らない。**いまの設定に無い値は黙って空にせず**「（いまの設定にありません）」と出す
- `D01 / D09 / D02` は配列ではなく**関数**＝ `pitCardMisses(c)` が返す**抜けている欄そのもの**を出す
  （必須／推奨の表は今までどおり `card-miss.js` の1本）
- 入れ子の欄は表が吸収する（例：`shakenDecided` → `c.inspSchedule.decided`）

### 🔴 わざと「ここを直す」を出さない規則がある

欄が1つに決まらないもの＝**タスクの列を動かす（T01/T02/T05/T06/T09）・代車カレンダー（L01〜L07）・
PIT配置図（R05/R06）・顧客とつなぐ（D07）・整備担当（T03）** など。
**小窓で直せるふりをすると、直したつもりで直っていない、が起きる。**
（載っていない規則は「ここを直す」が出ないだけで、黙って壊れることはない）

### 直したときに起きること

1. 変わった欄だけ書き込む（変わっていなければ「変わっていません」と言って閉じる）
2. **フローと操作ログに「何を どこから どこへ」** を必ず残す
   例：`データチェックから直した：カナ （空） → ケンサジロウ`
3. `PitDB.save()`
4. 🔴 **「直した」の札は自動で貼らない。**
   直したのに所見が残る＝**まだ直り切っていない**（別の欄も空、など）。
   そこへ自動で札を貼ると**残っている問題を自分で隠す**。本当に直っていれば、次の描き直しで所見が消える。それが答え。

### アーカイブ済みの車では

小窓の上に **「この車はアーカイブ済みです。データチェックが指摘した下の欄だけ直せます（ほかの欄は開きません）」** と出す。
＝ **黙って開けない。** なぜ触れるのかをその場で言う。

---

## ④🔴🔴 確定金額と確定日は、これまで通り管理者のみ

対象は **3つちょうど**：

| 欄 | 名前 |
|---|---|
| `amountFinal` | 確定金額（請求額） |
| `completedAt` | 実績カウント日 |
| `returnDateFinal` | 確定返車日 |

- 管理でない人には **入力欄が出ない。でも数字は見える**＋**「🔒 管理のみ」**の札
  （欄ごと消すと「無い」のか「触れない」のか分からなくなる）
- 🔴 **ボタンを押しても入らない**（画面から消しただけにしていない。保存する所でも同じ条件で止める）
- 🔴 `completedAt` は **空にできない**（空＝どの月にも数えられなくなる）

### 物差しは**借りる**（新しく作らない）

`card-view.js` に2つの口を新設して、`inspect-fix.js` はそれを**呼ぶだけ**。

```js
window.pitCanEditFinal    = canEditResultDate;      // ＝ !PIT_CLOUD || pitIsAdmin()
window.pitApplyResultDate = function (c, v) {       // 実績日・返車日・確定返車日の3つを必ず揃える
  c.completedAt = v; c.returnDate = v; c.returnDateFinal = v;
};
```

- ⚠ **`inspect-fix.js` に `pitIsAdmin` は1文字も書かない。** 書き写した日から、片方だけ直る事故が始まる
- `cvSetResultDate` も `pitApplyResultDate` を通すように直した（**入口が2つになったから手順は1本に**）

---

## 🧭 触ったファイル

| ファイル | 何をしたか |
|---|---|
| **`js/inspect-fix.js`** 🆕 | 直せる欄の表＋規則→欄の表＋小窓 |
| `js/inspect.js` | 名称変更／`MODES` の切り替え／クォーターの器／「ここを直す」ボタン |
| `css/inspect.css` | `.ins-mode*` `.ins-q-*` `.ins-fix*` を追加 |
| `js/card-view.js` | 🔴 `pitCanEditFinal` / `pitApplyResultDate` を**外へ貸した** |
| `js/sales.js` | 🔴 `pitQuarterOf` を**外へ貸した** |
| `js/inspect-rules.js` | 言葉の入れ替え＋「規則を足したら `RULE_FIX` にも1行」の覚え書き |
| `js/help-content.js` | データチェックの項を全面書き直し |
| `js/news-pit.js` | お知らせ 1件（`n-20260822-datacheck`）＝**運用ルール①よっぽど大きいもの**に当たると判断 |
| `js/views.js` / `index.html` | 見出し・メニュー・スクリプトタグ・版 **1.170.0** |

---

## 🧪 見張り

### `test_inspect.mjs` … 123 → **168 OK / 0 NG**

足したのは3節（44項目）：

- **⑫ 名前**＝メニュー・見出し・ボタン・帯が「データチェック」。
  🔴 **車のほうの「点検待ち」は言い換えていない**
- **⑬ 切り替え**＝2つある／**画面のいちばん上にある**／日常は規則が出る／
  🔴 **クォーターに切り替えると規則は出ない（別の中身）**／②③の話をしている／
  🔴 区切りは `pitQuarterOf` を借りている／8月9日は第2クォーター
- **⑭ ここを直す**
  - アーカイブ済みでも抜けは見つかる（D09）／🔴 **ボタンが出る（誰でも押せる）**／
    🔴 **「アーカイブ済み」と言ってから開く**
  - 🔴 **小窓に出るのは抜けている欄だけ**／🔴 **ナンバー・確定金額は出ない＝触れない**
  - 直せた／🔴 **直した欄以外は1文字も動いていない**／フローに残る／
    🔴 **直ったら所見が消える（札で隠すのではない）**／🔴 札は自動で貼らない
  - 🔴🔴 **`pitCanEditFinal` を差し替えて確かめる**（自前で判定していたら通らない）
    → 入力欄が出ない／数字は見える／「🔒 管理のみ」／**押しても入らない**／管理なら入る
  - 実績日も同じ守り＋🔴 **入れたら返車日も揃う**（`pitApplyResultDate` を通っている）
  - 🔴 **T01 には「ここを直す」を出さない**（欄が1つに決まらない）

### 🧭 物差しの見張り（機械が読む）

- 🔴🔴 **`inspect-fix.js` に `pitIsAdmin` が無く、`pitCanEditFinal` を呼んでいる**
- 🔴 `card-view.js` がその1本を貸している／実績日の手順も1本を借りている
- 🔴 画面（`inspect.js`）は直せる欄を**組み立てていない**（`PIT_FIX_FIELDS` を持たない）
- 🔴 画面がクォーターの区切りを書き写していない
- 🔴 表が指す規則・欄が**全部実在する**／欄の名前がダブっていない
- 🔴🔴 **管理者だけの欄は「確定金額・実績カウント日・確定返車日」の3つちょうど**（増えても減っても落ちる）

### `test_result_date.mjs` … 45 → **47 OK / 0 NG**

`_c.completedAt = v; … _c.returnDate = v;` という**字**を見ていた1本を、
**`pitApplyResultDate` の1本があること＋入口がそれを通っていること**に置き換えた（見張りの狙いは同じ）。

### PitFlow 見張り **103本すべて緑**（壊した所は無し）

⚠ 走らせ方の覚え書き（今回ハマった）
- 見張りは**それぞれ別のポート**を持っている（8934〜8999 のうち55個）。
  1つのサーバだけ立てても大半が `ERR_CONNECTION_REFUSED` で落ちる。**全ポート分立ててから回す**
- ⚠ `pkill -f "http.server"` は**自分のコマンド行にも当たって自分を殺す**。使わない

---

## 🚀 デプロイ（⚠ 前に間違えた形）

❌ `cd D:\Claude` → **`deploy-pitflow.ps1` は `D:\Claude\アプリ開発` の下**にある

```
powershell -File "D:\Claude\アプリ開発\deploy-pitflow.ps1" -Message "v1.170.0 点検→データチェックに改名／日常チェック・クォーターチェックの切り替え／アーカイブ車両でも指摘された欄だけ誰でも直せる「ここを直す」（確定金額・確定日は管理者のみ）"
```

**まだ未デプロイ。** ファイルは `D:\Claude\アプリ開発\PitFlow\pitflow` に反映済み（**削除ゼロ・`git status` 確認済み**）。
新規は `js/inspect-fix.js` の1本。

---

## ⏭ 次にやること（持ち越し）

1. 🔴 **判断待ち**：v1.169.0 で入れた「**まだ来ていない車の漢字の名前は言わない**」（電話受付でカナだけ＝正常。本番で71件中36件を埋めていた）を**戻すかどうか**。いまは**入れたまま**
2. **② 売上チェックリストPDF突合**の26件の確認（特に**Qまたぎ10台 1,675,057円**）
3. **③ を動かすために書き出しへ足すもの**＝母数（担当ごとの担当台数）／車の中身（フロー・メモ）／前回の書き出し
4. 62本目の規則の種：「**同じ日に入庫日／実績日が10台以上固まっている**」
5. 「ここを直す」を**出していない規則**のうち、`frontStaff` 以外にも選び直しで済むもの（`loanerId` など）を足すか
6. 棚卸し（前から持ち越し）：色 102／「1課2課」の直書き 20／見張りの日付の決め打ち 59
7. MHS が v1.42.0 に上がり `js/mhs-share.js` が変わっている件の確認（僕の作業ではない）

---

## 📌 覚えておくこと（この回で決まった作法）

- 🔴 **規則を足したら `inspect-fix.js` の `RULE_FIX` にも1行足す。**
  足さなければ「ここを直す」が出ないだけ＝黙って壊れない
- 🔴 **欄が1つに決まらない規則には出さない。** 直せるふりをしない
- 🔴 **管理者の判定・実績日を揃える手順・クォーターの区切りは、全部「借りる」。**
  書き写した瞬間に、片方だけ直る事故の種になる
- 🔴 **直したのに残る所見を、札で隠さない**
