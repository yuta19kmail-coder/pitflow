# 引き継ぎ 2026-08-18 ── 🔎 車検予定のホバーに「車検の詳細」（PitFlow **v1.128.0**）

> 🔴 **ゆうたはコードを読まない。** 話すときはファイル名・関数名・行番号を出さない。

---

## 🗣 依頼

> 「**車検予定のカードのホバーだけ**　基本情報の下に**車検の詳細　車検担当者　陸運局　R**　の表記を入れられない？」

---

## 🗂 やったこと

- ホバー情報カードの **基本情報（預かり／このフェーズ／代車の3タイル）のすぐ下**に
  「**車検の詳細**」の枠を足した。中身は **担当（回送）／陸運局／R（ラウンド）** の3行。
- 入っていないものは **「未定」**（薄い字）で出す。

### 🔴 決めごと
- 🔴 **車検予定の画面のホバーだけ**に出す（`state.currentView === 'shakencal'` かつ車検の車）。
  ⚠ 全部の画面に足すと、いつものホバーが**縦に伸びるだけ**になる。**ほかの画面には出さない。**
- 名前は **フルネーム**。ホバー情報カードは 2026-08-16 の決めごとで
  **「幅のある画面＝フルのまま」**の側（予約詳細と同じ扱い）。
  ⚠ 狭い枠（チップ・MHS・LINE）は v1.127.0 のとおり通称＆苗字。**ここだけ扱いが違う。**
- 中身の読み方は **pit-share.js の物差し**（`pitShakenStaffFull` / `pitShakenOffice` / `pitShakenRound`）。
  ⚠ ここで組み立て直さないこと。

---

## 🗂 触ったところ

| どこ | 何 |
|---|---|
| `pitflow\js\card-hover.js` | 基本情報の直後に「車検の詳細」の枠（車検予定の画面のときだけ） |
| `pitflow\css\card-hover.css` | `.ph-sec-shk` / `.ph-shk` / `.ph-shk-lb` / `.ph-shk-v`（未定は薄く） |
| `pitflow\index.html` | 版3か所を **v1.128.0**、`card-hover.js?v=39`・`card-hover.css?v=24` |
| `pitflow\test_shaken_ops.mjs` | 見張り **+7本**（54 OK） |

---

## ✅ 確かめたこと

`test_shaken_ops.mjs` ＝ **54 OK / 0 NG**（+7本）
- ホバーに「車検の詳細」が出る／🔴 **基本情報（3つのタイル）の下にある**（DOMの並びで確認）
- 担当・陸運局・R の3行が出る／入っていなければ「未定」

実際にマウスを乗せた画面でも、基本情報の下に3行が出ることを見た。
ほかも緑＝`test_shaken_unsched` 48 ／ `test_shaken_decide` 33 ／ `test_shaken_detail` 36 ／
`test_order_wash` 28 ／ `test_shaken_share` 27。

---

## 🚀 出しかた

```powershell
powershell -File "D:\Claude\アプリ開発\deploy-pitflow.ps1" -Message "v1.128.0 車検予定のホバーに車検の詳細（担当・陸運局・R）"
```

⚠ `_shared` と `firestore.rules` は触っていない。MHS・CarFlow も触っていない。

### 📣 お知らせ
**足していない**（見え方が増えるだけ）。

---

## ⏭ 次にやること

1. フロント担当・メカ担当など**ほかの担当名**も v1.127.0 の2つに揃えるか。
2. 済にする前の予約詳細にも陸運局とRを出すか／MHS・前日LINEにも出すか。
3. 🔴 未入庫（来なかった車）になると車検予定から黙って消える（v1.115.0 から持ち越し・要確認）
